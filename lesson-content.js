window.ILOVEOS_LESSONS = [
  {
    id: "cpu-architecture-data",
    module: "foundations",
    title: "CPU architecture and data representation",
    lead: "Learn the vocabulary underneath every later topic: instructions, registers, cores, address widths, binary, hexadecimal, and Windows data types.",
    core: [
      "A CPU repeatedly fetches, decodes, and executes machine instructions. Registers hold the values the current instruction needs, while RAM holds a much larger working set. A process does not execute by itself, one or more threads supply the instruction stream and register context that a core can run.",
      "Humans usually count in decimal. Digital computers represent state with bits, so binary is natural. Hexadecimal is a compact way to write binary: one hex digit represents exactly four bits. Windows tools use hex for addresses, bit flags, access masks, and raw file structures because boundaries are easier to see."
    ],
    mechanics: [
      "Architecture describes the instruction set and execution environment, such as x86 or x64. Two CPU packages with four physical cores each provide eight physical cores. That is not a promise of eight instructions per clock, because modern cores pipeline, reorder, and sometimes expose multiple logical processors.",
      "An N-bit address can encode 2^N distinct values. A simple 32-bit byte-address calculation gives 2^32 bytes, or 4 GiB. Real usable address space also depends on the operating system, process model, hardware, and reserved regions. On 64-bit Windows, current processors implement fewer than all 64 virtual-address bits."
    ],
    windows: [
      "Win32 declarations use aliases such as BYTE, WORD, DWORD, UINT, BOOL, HANDLE, LPVOID, and LPCTSTR. The name communicates width, meaning, pointer level, and constness. LPCTSTR is a pointer to constant TCHAR text, so it is not a numeric value. Pointer-sized types such as ULONG_PTR change width between 32-bit and 64-bit builds.",
      "In Python, pywin32 converts many declarations for you. With ctypes you must declare argtypes, restype, structures, pointer types, and the Unicode or ANSI function variant yourself. A wrong type can truncate a pointer or corrupt memory, so the native declaration remains part of the task."
    ],
    keys: ["Binary stores state with bits", "Hexadecimal groups four bits per digit", "A 32-bit byte address has 2^32 possible values", "Pointer-sized types depend on process architecture"],
    apis: ["ctypes.sizeof", "ctypes.wintypes", "IsWow64Process2", "GetNativeSystemInfo"],
    practice: {
      title: "Decode what Windows shows you",
      time: "15 min",
      intro: "Connect notation to a live process before relying on it in later memory and PE lessons.",
      steps: ["Open Process Explorer and add the Image Type and Start Address columns.", "Pick one 64-bit process and, if available, one 32-bit process. Record their architecture and one hexadecimal address.", "Convert the final two hex digits of each address to binary.", "Use Python to print ctypes.sizeof(ctypes.c_void_p) and explain why the result follows the Python process architecture.", "Write one sentence distinguishing CPU package, physical core, logical processor, process, and thread."]
    },
    check: ["Which item is not a numeric Win32 type?", ["BOOL", "CHAR", "UINT", "LPCTSTR"], 3, "LPCTSTR is a pointer to constant TCHAR text. The other choices directly represent numeric values, even when Windows uses them for Boolean or character semantics."],
    sources: [["Windows data types", "https://learn.microsoft.com/windows/win32/winprog/windows-data-types"], ["x64 architecture", "https://learn.microsoft.com/windows-hardware/drivers/debugger/x64-architecture"]]
  },
  {
    id: "why-operating-system",
    aliases: ["os-foundations"],
    module: "foundations",
    title: "Why does an operating system exist?",
    lead: "An operating system makes hardware usable, shares scarce resources, and creates protected abstractions that applications can rely on.",
    core: [
      "Without an operating system, every program would need to understand the exact processor, storage controller, display, keyboard, and network device. The OS and its drivers turn hardware-specific mechanisms into stable concepts such as files, processes, sockets, and windows.",
      "The OS is also a resource manager. It decides which ready thread receives a processor, which virtual pages are resident in RAM, who may open an object, and when cached data reaches storage. These decisions let unrelated programs run together without having to cooperate directly."
    ],
    mechanics: [
      "Multiprogramming began as a way to keep expensive processors useful while one job waited for slow I/O. Preemptive scheduling adds a stronger rule: the OS can pause a running thread and choose another. Memory protection prevents one ordinary process from simply overwriting another process or the kernel.",
      "The useful mental model is intent, interface, policy, mechanism. An application states an intent through an API. Windows validates the request under its policies, chooses a mechanism, and returns a result or error."
    ],
    windows: [
      "Windows exposes several layers. Applications usually call the documented Win32 API. User-mode libraries may complete the operation themselves or call the Native API in Ntdll. A system call transfers control to kernel code when protected operating-system work is required.",
      "pywin32 presents many Win32 operations as Python functions. ctypes can call exported C functions that pywin32 does not wrap, but it also makes the caller responsible for types, buffers, return-value checks, and ownership."
    ],
    keys: ["The OS abstracts hardware", "It allocates and protects shared resources", "Scheduling hides I/O waits and shares processors", "APIs express intent while the OS applies policy"],
    apis: ["win32api.GetCurrentProcessId", "win32api.GetUserName", "win32api.GetVersionEx"],
    practice: {
      title: "Find Python inside Windows",
      time: "10 to 15 min",
      download: ["downloads/who_am_i.py", "who_am_i.py"],
      intro: "Run a tiny pywin32 program and identify the operating-system state that exists around it.",
      steps: ["Predict the parent process and user before running the script.", "Open Process Explorer and enable Process ID, User Name, Integrity Level, and Image Type.", "Run who_am_i.py from a terminal and find its matching PID.", "Inspect the process path, parent, token, threads, and loaded modules.", "Classify each observation as program data, an OS abstraction, or an OS-managed resource."]
    },
    check: ["Why can Windows run another thread while a music player waits for storage?", ["The first process lost its memory", "The processor would otherwise be idle", "Every I/O request terminates a thread", "The file became a process"], 1, "A waiting thread is not ready to use a processor. The scheduler can dispatch a ready thread and keep the hardware doing useful work."],
    sources: [["Windows architecture", "https://learn.microsoft.com/windows-hardware/drivers/gettingstarted/windows-architecture"]]
  },
  {
    id: "windows-organisation",
    module: "foundations",
    title: "How Windows is organised",
    lead: "Build a map of Windows from user applications down through subsystems, executive components, the kernel, drivers, and hardware.",
    core: [
      "Modern desktop Windows belongs to the Windows NT family. User-mode applications rely on subsystem DLLs such as Kernel32, Advapi32, User32, and Gdi32. Many lower-level calls converge on Ntdll, which provides the user-mode side of the Native API and system-call transition.",
      "Kernel mode contains the executive, kernel, device drivers, and hardware abstraction support. Executive components implement object management, process and thread management, virtual memory, I/O, security, configuration, and other operating-system services. The kernel focuses on low-level scheduling, traps, interrupts, and synchronisation."
    ],
    mechanics: [
      "A request often crosses several components. CreateFileW begins in a user-mode DLL, enters the kernel through a native service, passes through the I/O Manager, and travels down a driver stack. The returned handle is placed in the caller's handle table and represents access to a kernel object.",
      "Not every API call crosses into the kernel. Some functions calculate, format, or inspect user-mode state. Treat the path as something to investigate, not something to infer from the library name alone."
    ],
    windows: [
      "Core system processes expose the architecture in practice. System hosts kernel-backed activity, services.exe manages services, lsass.exe supports security policy and authentication, and svchost.exe hosts groups of DLL-based services. Exact process arrangements vary by Windows version and configuration.",
      "Process Explorer shows process ancestry, users, integrity, threads, handles, and modules. WinObj shows the Object Manager namespace. Process Monitor shows file, Registry, process, thread, and image-load operations moving through Windows."
    ],
    keys: ["Win32 is primarily an application-facing interface", "Ntdll connects user-mode native calls to system calls", "Executive components manage major OS resources", "Drivers participate in I/O paths"],
    apis: ["CreateFileW", "NtCreateFile", "CloseHandle", "QueryDosDeviceW"],
    practice: { title: "Trace one request through the layers", time: "20 min", intro: "Use evidence to replace the idea that Windows is one opaque block.", steps: ["Start Process Monitor as administrator and clear existing events.", "Filter to one test process and the CreateFile operation.", "Open a harmless text file in that process.", "Inspect the event stack and note user-mode modules, the system-call boundary, and drivers shown.", "Draw the request as application, API DLL, Native API, kernel manager, driver, object, then label the returned handle."] },
    check: ["Which component normally contains the user-mode system-call stubs?", ["Ntdll.dll", "Explorer.exe", "Services.exe", "WinObj.exe"], 0, "Ntdll exposes the Native API and contains the user-mode transition code for system services."],
    sources: [["Windows architecture", "https://learn.microsoft.com/windows-hardware/drivers/gettingstarted/windows-architecture"]]
  },
  {
    id: "user-kernel-mode",
    module: "foundations",
    title: "User mode and kernel mode",
    lead: "Understand the protection boundary that limits ordinary applications and gives trusted OS code controlled access to the whole machine.",
    core: [
      "User mode and kernel mode are processor-enforced privilege levels. Application code normally runs in user mode, where it cannot execute privileged instructions or directly access kernel memory. Windows kernel code and most drivers run in kernel mode and can access protected resources.",
      "This boundary contains failures. A bad user-mode pointer should normally terminate or fault one process. A faulty kernel-mode driver can corrupt system-wide state and crash the machine. The distinction is about execution privilege, not whether the person logged in is an administrator."
    ],
    mechanics: [
      "A system call is a controlled transfer. The calling thread supplies a service number and arguments, the processor switches to a kernel entry point, Windows validates user-supplied addresses and permissions, performs the operation, and returns to user mode.",
      "Exceptions and interrupts also transfer control. A page fault may be expected and resolved by the memory manager. A hardware interrupt lets a device demand attention. Windows restores a thread context when it is ready to resume."
    ],
    windows: [
      "An administrator application still runs in user mode. Elevation changes the token and permissions available to access checks; it does not move the program into kernel mode. Conversely, a driver executes in kernel mode even when it serves an unprivileged caller, so it must validate everything received from user mode.",
      "Protected Process Light and virtualization-based security add boundaries beyond the basic two-mode model. Learn the ordinary boundary first, then treat those as additional policy and isolation layers."
    ],
    keys: ["Execution mode and user identity are different concepts", "System calls are controlled privilege transitions", "Kernel bugs have system-wide impact", "Drivers must distrust user-mode input"],
    apis: ["IsUserAnAdmin", "OpenProcessToken", "DeviceIoControl", "GetLastError"],
    practice: { title: "Compare standard and elevated contexts", time: "15 min", intro: "Observe that elevation changes security context without changing processor execution mode.", steps: ["Open one ordinary terminal and one elevated terminal.", "Start the same Python pause script from both.", "Compare User, Integrity, UAC Virtualization, and token details in Process Explorer.", "Confirm that both processes are still ordinary user-mode applications.", "Explain which later access checks may differ and why."] },
    check: ["What changes when an application is elevated?", ["Its code permanently runs in kernel mode", "Its token and effective permissions change", "Its CPU architecture changes", "Its system calls are disabled"], 1, "Elevation supplies a higher-integrity token with additional group and privilege state. The application remains user-mode code."],
    sources: [["User mode and kernel mode", "https://learn.microsoft.com/windows-hardware/drivers/gettingstarted/user-mode-and-kernel-mode"]]
  },
  {
    id: "system-calls-win32",
    module: "foundations",
    title: "System calls and the Win32 API",
    lead: "Separate the public programming interface from the lower-level transition that asks the kernel to perform protected work.",
    core: [
      "The Win32 API is a broad C programming interface documented for applications. Its functions live across DLLs such as Kernel32, Advapi32, User32, and Ws2_32. A Win32 function may validate or transform arguments, perform work in user mode, call another service, or eventually invoke the Native API.",
      "A system call is the specific transition into the kernel. One public API can make zero, one, or several system calls, and Microsoft may change the internal route between versions. Build code against documented contracts, not an assumed call path."
    ],
    mechanics: [
      "CreateFileW is a useful example. The W suffix selects UTF-16 text. Its parameters describe the path, requested access, sharing, creation behavior, flags, security, and an optional template. Success returns a handle; failure returns INVALID_HANDLE_VALUE and sets the thread's last-error code.",
      "pywin32 often turns failure into pywintypes.error and converts handles or structures into Python objects. ctypes exposes the C boundary more directly. Configure argtypes and restype before the call, preserve the last error, test the documented failure sentinel, and release returned resources correctly."
    ],
    windows: [
      "MSDN is now presented through Microsoft Learn, and it matters for both pywin32 and ctypes. pywin32 changes Python spelling and representation, but the native documentation still defines the operation, access rights, flags, lifetime, and security requirements.",
      "Use pywin32 first when it has a clear wrapper. Use ctypes for an uncovered function, exact ABI study, callback, structure, or pointer-level task. Do not assume every Win32 function has a pywin32 wrapper."
    ],
    keys: ["Win32 API calls and system calls are not synonyms", "Native documentation matters for pywin32 and ctypes", "Unicode W variants are the modern default", "Failure and cleanup are part of every API contract"],
    apis: ["CreateFileW", "win32file.CreateFile", "GetLastError", "ctypes.WinDLL"],
    practice: { title: "Compare a wrapper with the native declaration", time: "20 min", intro: "Read the same operation from both sides of the Python boundary.", steps: ["Open the Microsoft Learn page for CreateFileW and the pywin32 page for win32file.CreateFile.", "Map each native parameter to the Python wrapper argument.", "Identify the success value, failure behavior, required access flags, and cleanup function.", "Write a minimal read-only call in pseudocode with a try/finally cleanup block.", "Explain one conversion pywin32 performs for you."] },
    check: ["Which statement is correct?", ["Every Win32 call is a system call", "All Win32 APIs are wrapped by pywin32", "Microsoft Learn is useful with pywin32 and ctypes", "ctypes automatically infers every C signature"], 2, "The native contract defines behavior for either binding. Wrappers do not remove the need to understand that contract."],
    sources: [["Windows API index", "https://learn.microsoft.com/windows/win32/apiindex/windows-api-list"], ["CreateFileW", "https://learn.microsoft.com/windows/win32/api/fileapi/nf-fileapi-createfilew"]]
  },
  {
    id: "reading-winapi-docs",
    module: "foundations",
    title: "How to read and call a Windows API",
    lead: "Turn a C declaration into a safe Python call by reading direction annotations, types, return values, errors, and ownership as one contract.",
    core: [
      "Start with purpose and requirements. Confirm the correct function family, minimum supported client, header, library, and DLL. Then read every parameter. [in] means the caller supplies initialized data, [out] means the function writes a result, and [in, out] means both. optional qualifies a direction, for example [in, optional].",
      "Translate types by meaning. DWORD is a 32-bit unsigned value, BOOL is an integer truth value, HANDLE is pointer-sized and opaque, LPCWSTR points to read-only UTF-16 text, LPVOID is an untyped pointer, and a structure pointer must match the documented layout and lifetime."
    ],
    mechanics: [
      "Next read the return-value section before writing the call. Failure might be zero, NULL, INVALID_HANDLE_VALUE, a special status code, HRESULT, or a count with another sentinel. Only call GetLastError when the documentation says the function sets it, and capture it immediately because another API can overwrite the thread-local value.",
      "Finally decide ownership. Ask whether the returned handle, allocated buffer, library reference, device context, or COM interface must be released, and with which matching function. CloseHandle is common but not universal. Registry keys, service handles, DLL references, sockets, and local allocations have their own release functions."
    ],
    windows: [
      "pywin32 wrappers normally raise pywintypes.error for Win32 failures. Catch it only where you can add useful context or recover, inspect winerror, funcname, and strerror, and still close resources in finally. Wait functions are different: WAIT_OBJECT_0, WAIT_TIMEOUT, WAIT_ABANDONED, and WAIT_FAILED are results that your logic must interpret.",
      "With ctypes, use WinDLL(..., use_last_error=True), set argtypes and restype, test the documented failure value, then raise ctypes.WinError(ctypes.get_last_error()). For output structures, allocate the structure first and pass ctypes.byref(instance)."
    ],
    keys: ["Read parameters, result, failure, and ownership together", "Direction annotations describe data flow", "Not every failure uses the same sentinel", "Wait results are control flow, not ordinary exceptions"],
    apis: ["ctypes.WinDLL", "ctypes.get_last_error", "ctypes.WinError", "win32event.WaitForSingleObject", "win32api.CloseHandle"],
    practice: { title: "Build an API contract card", time: "25 min", intro: "Create a reusable checklist for an API you will meet later.", steps: ["Choose ControlService or VirtualQueryEx in Microsoft Learn.", "Record the DLL, Python binding, parameter names, direction, C type, and Python type.", "Record the success result, every relevant non-success result, and how to retrieve error detail.", "List every acquired resource and its matching cleanup operation.", "Write both a pywin32-style and ctypes-style error-handling outline."] },
    check: ["What should code do after WaitForSingleObject returns WAIT_TIMEOUT?", ["Treat it as a successful acquisition", "Call CloseHandle on the object immediately", "Handle the timeout branch explicitly", "Assume pywintypes.error was raised"], 2, "A timeout is a documented wait result. The program should take its timeout path without pretending the object was acquired."],
    sources: [["Windows data types", "https://learn.microsoft.com/windows/win32/winprog/windows-data-types"], ["GetLastError", "https://learn.microsoft.com/windows/win32/api/errhandlingapi/nf-errhandlingapi-getlasterror"]]
  },
  {
    id: "inspect-windows",
    module: "foundations",
    title: "Inspecting the operating system",
    lead: "Learn to turn a question into evidence with Process Explorer, Process Monitor, WinObj, command-line tools, and small Python probes.",
    core: [
      "A tool is useful when it answers a precise question. Process Explorer is strongest for current process, thread, handle, module, token, and resource state. Process Monitor records file, Registry, process, thread, and image-load events. WinObj exposes the named Object Manager namespace.",
      "Snapshots and traces answer different questions. A snapshot says what exists now. A trace says what happened over time. If a short-lived handle is absent from Process Explorer, a Process Monitor trace may still show the create, use, and close operations."
    ],
    mechanics: [
      "Use a repeatable investigation loop: state a hypothesis, choose the artifact that would distinguish explanations, minimize background noise, capture a short interval, preserve relevant columns and timestamps, then explain what the evidence does and does not prove.",
      "Filters do not change the system, but they can hide evidence. Start narrow enough to work, then relax one condition when the expected event is missing. Keep process ID and thread ID visible because names can be reused and concurrent work interleaves."
    ],
    windows: [
      "Sysinternals tools often need elevation to inspect protected processes or system-wide events. Lack of access is evidence about the security boundary, not a reason to disable protections. Use an isolated VM for experiments that alter services, startup behavior, or executable files.",
      "A Python probe supplies ground truth from inside the process, while an external tool supplies an observer's view. Matching PID, TID, handle name, path, or timestamp connects the two."
    ],
    keys: ["Begin with a question, not a tool", "Snapshots show state while traces show history", "PID and TID connect evidence", "Access denied is part of the model"],
    apis: ["win32api.GetCurrentProcessId", "win32api.GetCurrentThreadId", "win32process.EnumProcesses", "win32process.EnumProcessModules"],
    practice: { title: "Run a three-view investigation", time: "25 min", intro: "Observe one harmless script from inside, from a live snapshot, and from a trace.", steps: ["Write a script that prints its PID, creates a temporary file, pauses, and then closes it.", "Find the process and open file handle in Process Explorer.", "Capture only that PID in Process Monitor and observe CreateFile, WriteFile, and CloseFile.", "Record which facts appeared in the script, snapshot, and trace.", "Write one limitation for each evidence source."] },
    check: ["Which tool is the clearest first choice for a chronological file-operation trace?", ["Process Monitor", "WinObj", "RAMMap", "Sigcheck"], 0, "Process Monitor records timestamped file-system and other operating-system activity that can be filtered to the target process and path."],
    sources: [["Process Explorer", "https://learn.microsoft.com/sysinternals/downloads/process-explorer"], ["Process Monitor", "https://learn.microsoft.com/sysinternals/downloads/procmon"]]
  },

  {
    id: "programs-processes-isolation",
    module: "processes-handles",
    title: "Programs, processes, and isolation",
    lead: "A program is passive code on disk. A process is the protected execution container Windows builds around that code.",
    core: [
      "A process owns an isolated virtual address space and a collection of resources needed to run a program. It has a process ID, executable image, handle table, security token, environment, current directory, and at least one thread. The executable file is only one input to this runtime object.",
      "Two processes running the same executable are still separate. They have different PIDs, address spaces, handle tables, tokens or token state, and lifetimes. Sharing data requires an explicit IPC or inherited/shared object relationship."
    ],
    mechanics: [
      "Isolation is implemented mainly with virtual memory and access checks. The same virtual address in two processes can translate to different physical pages. A process cannot simply dereference another process's pointer. Cross-process operations require a handle with suitable access rights and an API designed for that purpose.",
      "A parent process can create a child, but process trees describe creation relationships rather than permanent ownership. The child may outlive the parent, and Windows does not automatically terminate an arbitrary descendant tree unless a Job Object or application policy provides that relationship."
    ],
    windows: [
      "Process Explorer shows process trees and per-process properties. Task Manager provides a simpler view. win32process.EnumProcesses returns PIDs, while OpenProcess turns a PID into a checked handle that grants a specific set of operations.",
      "PID values are reused. Do not treat a PID alone as a durable identity across time; pair it with creation time or a live handle when correctness matters."
    ],
    keys: ["A program is not a process", "Processes isolate address spaces and resources", "Cross-process access is explicit and checked", "A process tree records creation ancestry"],
    apis: ["win32process.EnumProcesses", "win32api.OpenProcess", "win32process.GetProcessTimes", "win32job.CreateJobObject"],
    practice: { title: "Separate image from instance", time: "15 min", intro: "Use two instances of the same program to identify what belongs to the file and what belongs to each process.", steps: ["Start two instances of Notepad.", "Compare their image path, PID, creation time, handles, threads, and working set in Process Explorer.", "Find one property shared because the image is the same and three properties unique to each instance.", "Close one process and confirm the executable file still exists and the other process continues.", "Explain why a PID is not a filename."] },
    check: ["Which item is guaranteed to differ between two simultaneous processes?", ["Executable path", "Process ID", "Imported DLL names", "Publisher signature"], 1, "Every live process has a distinct PID, while two instances may use the same executable and many of the same modules."],
    sources: [["Processes and threads", "https://learn.microsoft.com/windows/win32/procthread/processes-and-threads"]]
  },
  {
    id: "process-address-space",
    module: "processes-handles",
    title: "Inside a process",
    lead: "Map the major pieces of a process: virtual memory, executable image, DLLs, heaps, stacks, environment, token, handles, and threads.",
    core: [
      "The address space contains mapped executable sections, DLL images, thread stacks, heaps, private allocations, shared sections, and operating-system data. These regions have different state, type, protection, and backing. A high private-byte count and a high working set describe different things.",
      "The process object records process-wide state, but threads are the schedulable entities. Each thread has its own stack, register context, state, priority, and thread-local storage while sharing most process resources."
    ],
    mechanics: [
      "The process environment block stores user-mode process metadata, and each thread has a thread environment block. These are implementation-facing structures, not replacements for documented APIs. Windows also associates a primary access token with the process and may associate an impersonation token with a thread.",
      "Handles are entries in a per-process table. The numeric value is meaningful only in the owning process unless explicitly duplicated or inherited. The table entry records granted access and points to an underlying kernel object."
    ],
    windows: [
      "Process Explorer's Properties dialog separates Image, Threads, TCP/IP, Security, Environment, Strings, and other views. VMMap categorizes the address space. The lower pane can show handles or DLLs, which are different namespaces and should not be confused.",
      "A complete process investigation uses more than one metric. CPU time belongs to threads but aggregates at process level; I/O counts represent operations; private bytes represent committed private memory; working set represents currently resident pages."
    ],
    keys: ["Threads execute inside a process container", "Address-space regions have different purposes", "Handles and DLLs are different views", "Security context is part of process state"],
    apis: ["win32process.GetProcessMemoryInfo", "win32process.EnumProcessModules", "win32api.GetEnvironmentVariable", "win32security.OpenProcessToken"],
    practice: { title: "Build a process inventory", time: "20 min", intro: "Describe one Python process using each major category of process state.", steps: ["Run a Python script that allocates a list, opens a file, and creates one worker thread before pausing.", "Use Process Explorer to record PID, parent, user, integrity, thread count, handle count, and private bytes.", "Use the lower pane to find the open file handle and several DLLs.", "Open VMMap and identify image, heap, stack, and private-data regions.", "Explain which resource disappears when the file is closed and which remain until process exit."] },
    check: ["Which item belongs separately to each thread?", ["Virtual address space", "Handle table", "Register context and stack", "Primary executable image"], 2, "Each thread needs its own execution context and stack. Threads in one process normally share its address space and handle table."],
    sources: [["Process objects", "https://learn.microsoft.com/windows/win32/procthread/processes-and-threads"]]
  },
  {
    id: "context-switch-process-metadata",
    module: "processes-handles",
    title: "Context switches and process metadata",
    lead: "Understand what Windows preserves when execution moves and which identifiers let you correlate activity across tools.",
    core: [
      "A context switch saves the running thread's processor state and restores another thread's state. Registers, instruction pointer, stack pointer, and scheduling state matter directly. Process address-space context may also change, although modern processors cache address translations to reduce the cost.",
      "The scheduler switches threads, not whole processes. A switch between two threads in the same process can reuse more process-wide state than a switch across processes, but every switch still has overhead and can disturb CPU caches."
    ],
    mechanics: [
      "Useful identifiers include PID, TID, parent PID, session ID, creation time, image path, and command line. A TID is system-wide while live, but like a PID it can be reused later. Timestamps and a live handle make correlations stronger.",
      "CPU time is accumulated execution time, not wall-clock age. A process can exist for ten minutes but consume only milliseconds of CPU while waiting. Context-switch counts show scheduling activity, not necessarily productive work."
    ],
    windows: [
      "Process Explorer can add Context Switch Delta and CPU Time columns. Process Monitor records PID and TID for each event. Matching them explains which thread issued a file or Registry operation and whether work is distributed across workers.",
      "Performance improves when blocking work overlaps or computation uses available cores. It degrades when excessive threads add scheduling, memory, and cache overhead. Measurements must use a representative workload."
    ],
    keys: ["Windows schedules threads", "Context switching preserves execution state", "CPU time differs from elapsed time", "Use PID, TID, and time together"],
    apis: ["win32api.GetCurrentThreadId", "win32process.GetProcessTimes", "win32process.GetThreadTimes", "QueryPerformanceCounter"],
    practice: { title: "Correlate work by thread ID", time: "20 min", intro: "Recreate the evidence pattern used by the better_together investigation.", steps: ["Run a file-processing workload once with one worker and once with several workers.", "Capture only that process in Process Monitor with TID visible.", "Compare elapsed time, unique worker TIDs, and interleaving of CreateFile, ReadFile, and CloseFile.", "Repeat with a very small directory and explain why multiple workers may lose.", "State whether the evidence proves CPU parallelism, I/O overlap, or only concurrent issue of operations."] },
    check: ["What does process CPU time measure?", ["The process's age", "Accumulated time its threads executed", "Time spent waiting for all I/O", "Number of context switches"], 1, "Process CPU time sums execution time used by its threads. Waiting and elapsed lifetime are different measurements."],
    sources: [["Context switches", "https://learn.microsoft.com/windows/win32/procthread/context-switches"]]
  },
  {
    id: "createprocess-lifecycle",
    module: "processes-handles",
    title: "How Windows creates a process",
    lead: "Trace process creation from an executable path and command line to a new address space, initial thread, inherited state, and loader startup.",
    core: [
      "CreateProcess creates a process object and an initial thread. Windows validates the image, creates an address space, maps the executable and Ntdll, establishes process parameters and security context, then lets the initial thread begin user-mode loader initialization.",
      "The call returns a process handle, thread handle, PID, and TID. The handles belong to the creator and must be closed even though the child keeps running. The IDs are identifiers, not ownership-bearing handles."
    ],
    mechanics: [
      "Application name and command line are separate parameters with security-sensitive parsing rules. Handle inheritance requires both a globally inheritable handle and bInheritHandles set for the creation call, or an explicit handle list in extended startup attributes. Accidental inheritance can keep files or pipes open indefinitely.",
      "STARTUPINFO controls window and standard-handle behavior. Creation flags select options such as a new console, suspended initial thread, or Unicode environment. The current directory and environment shape later file and DLL lookup."
    ],
    windows: [
      "pywin32process.CreateProcess returns a four-item tuple. Use try/finally to close both returned handles. To wait for completion, wait on the process handle and then query its exit code. A process object remains until the last handle reference is released.",
      "The Hidden_Process practice uses ctypes structures and STARTF_USESHOWWINDOW to influence the initial window. That hides a window, not the process. Process Explorer, Task Manager details, and process enumeration can still reveal it."
    ],
    keys: ["Process creation also creates the initial thread", "Returned handles must be closed", "Handle inheritance requires explicit conditions", "A hidden window is not a hidden process"],
    apis: ["win32process.CreateProcess", "win32process.STARTUPINFO", "win32event.WaitForSingleObject", "win32process.GetExitCodeProcess"],
    practice: { title: "Create, observe, wait, and clean up", time: "30 min", intro: "Build a safe launcher that makes every lifetime transition visible.", steps: ["Download or recreate the hidden_process.py scaffold from the repository practice.", "Define STARTUPINFO and PROCESS_INFORMATION with exact ctypes field order and pointer-sized handles.", "Launch Notepad with and without STARTF_USESHOWWINDOW and SW_HIDE.", "Find both processes externally and explain what was hidden.", "Wait on the process handle, obtain its exit code, and close both process and thread handles in finally."] },
    check: ["Which resource must the parent close after CreateProcess succeeds?", ["Only the PID", "The returned process and thread handles", "The child's executable file", "The child's virtual address space"], 1, "CreateProcess returns two handles owned by the caller. Closing them releases the caller's references without necessarily terminating the child."],
    sources: [["CreateProcessW", "https://learn.microsoft.com/windows/win32/api/processthreadsapi/nf-processthreadsapi-createprocessw"]]
  },
  {
    id: "object-manager",
    module: "processes-handles",
    title: "Kernel objects and the Object Manager",
    lead: "See how Windows gives files, processes, events, sections, and other resources a common kernel-managed object model.",
    core: [
      "A Windows executive object is a kernel data structure representing a resource. Process, thread, file, token, event, mutex, semaphore, section, registry key, and Job objects participate in related management patterns even though their operations differ.",
      "The Object Manager creates, names, protects, tracks, and deletes many object types. User-mode code does not receive a kernel pointer. It receives a handle whose table entry refers to the object and records granted access."
    ],
    mechanics: [
      "Named objects can appear in the Object Manager namespace under paths such as BaseNamedObjects. Local and Global prefixes affect terminal-session namespaces for several synchronization and file-mapping objects. Name lookup lets unrelated processes open the same object when security allows it.",
      "Object names, DOS paths, and Win32 paths are related but not identical namespaces. Symbolic links such as drive letters translate familiar paths into Object Manager device paths. WinObj makes these relationships visible."
    ],
    windows: [
      "WinObj can browse object types, directories, symbolic links, devices, sections, and named synchronization objects. Create a named event in Python and observe its appearance and disappearance as handles are opened and closed.",
      "Not every kernel resource is named, and not every Win32 handle is closed with CloseHandle. The API family defines the correct lifetime rule."
    ],
    keys: ["Objects represent resources in kernel memory", "Handles are checked user-mode references", "Names let processes find shared objects", "Object namespaces differ from ordinary file paths"],
    apis: ["win32event.CreateEvent", "win32event.OpenEvent", "win32file.CreateFile", "win32api.QueryDosDevice"],
    practice: { title: "Watch a named object live", time: "20 min", intro: "Create an object from Python and find it in both the namespace and a process handle table.", steps: ["Create a manual-reset event named Local\\ILOVEOS_ObjectLab and pause the script.", "Find the event under BaseNamedObjects in WinObj.", "Find the Event handle in Process Explorer's lower pane.", "Open the same event from a second Python process.", "Close one handle, then the other, and explain when the object disappears."] },
    check: ["What does a user-mode handle directly contain?", ["A portable filename", "A raw kernel pointer", "A process-table value interpreted by Windows", "The complete object data structure"], 2, "The numeric handle indexes or identifies an entry in the process handle table. Windows uses that entry to reach the object and granted-access state."],
    sources: [["Object Manager", "https://learn.microsoft.com/windows-hardware/drivers/kernel/windows-kernel-mode-object-manager"]]
  },
  {
    id: "handles-rights-lifetime",
    module: "processes-handles",
    title: "Handles, access rights, and reference counting",
    lead: "Treat every handle as a capability with a scope, granted rights, inheritance policy, and lifetime.",
    core: [
      "Opening an object combines identity with requested access. The access check compares the caller's security context, requested mask, and the object's security descriptor. On success, the handle table entry records the rights actually granted. A later operation succeeds only if that handle includes its required rights.",
      "Least privilege applies to handles. PROCESS_QUERY_LIMITED_INFORMATION is safer and more likely to succeed than PROCESS_ALL_ACCESS when the task only needs an image path. Asking for unnecessary rights creates avoidable failures and expands impact if the handle is misused."
    ],
    mechanics: [
      "The object usually survives while references remain. Closing one handle decrements the relevant count; it does not necessarily destroy the resource because other handles or internal references can remain. Leaked handles keep objects and related storage alive longer than intended.",
      "DuplicateHandle creates another handle, possibly in another process, with selected rights. Inheritance transfers selected handles during process creation. Both are deliberate ways to cross process handle-table boundaries."
    ],
    windows: [
      "Process Explorer and Handle can identify which process keeps a file or named object open. Process Monitor can compare efficient file lifetime with a program that repeatedly opens and closes, or repeatedly forces FlushFileBuffers and defeats normal caching.",
      "The good, bad, and ugly practices teach two different costs: excessive handle churn and excessive durability barriers. A trace must distinguish CreateFile, WriteFile, CloseFile, and FlushBuffersFile rather than grouping everything as disk activity."
    ],
    keys: ["A handle carries granted access", "Request only the rights required", "Closing releases one reference", "Inheritance and duplication are explicit transfers"],
    apis: ["win32api.CloseHandle", "win32api.DuplicateHandle", "win32api.SetHandleInformation", "win32file.FlushFileBuffers"],
    practice: { title: "Explain file lifetime from a trace", time: "30 min", intro: "Repeat the repository's good and bad logging investigations with a stronger evidence standard.", steps: ["Capture GoodLog.exe and BadLog.exe separately in Process Monitor.", "Filter by process name and log path, then count CreateFile, WriteFile, CloseFile, and FlushBuffersFile operations.", "Separate the cost of open/close churn from forced cache flushes.", "Use Handle or Process Explorer to catch the file while it is open.", "State what the trace proves, what elapsed timing adds, and what storage caching prevents you from assuming."] },
    check: ["Why can closing a handle fail to destroy its object?", ["Handles never affect lifetime", "Other handles or kernel references may remain", "The PID always owns the object forever", "CloseHandle only works on files"], 1, "Executive objects are reference tracked. The object can remain until all relevant handle and internal references are gone."],
    sources: [["Handles and objects", "https://learn.microsoft.com/windows/win32/sysinfo/handles-and-objects"], ["DuplicateHandle", "https://learn.microsoft.com/windows/win32/api/handleapi/nf-handleapi-duplicatehandle"]]
  },
  {
    id: "threads-in-processes", module: "threads-scheduling", title: "Processes and their threads",
    lead: "A process owns the execution environment, but threads are the entities Windows actually schedules on processors.",
    core: ["Every process starts with at least one thread. Threads share the process address space, executable code, heaps, and handles, which makes communication cheap but also makes accidental interference possible.", "Each thread owns a register context, user and kernel stacks, thread-local storage, scheduling state, and a thread ID. A blocked thread does not prevent another ready thread in the same process from running."],
    mechanics: ["Concurrency means tasks make progress during overlapping periods. Parallelism means instructions actually execute at the same time on different logical processors. Multiple threads provide an opportunity for both, not a guarantee.", "Threads help when work blocks on I/O or when CPU work can be divided across cores. They hurt when setup, coordination, context switching, cache pressure, or contention costs more than the useful overlap."],
    windows: ["Process Explorer's Threads tab shows TID, CPU, start address, state-related information, and stack traces when symbols are available. Process Monitor can show which TID issued each file operation.", "Python's threading module uses native Windows threads, but CPython's global interpreter lock limits simultaneous execution of ordinary Python bytecode. Blocking I/O and native extensions can still overlap, while CPU-bound Python often needs processes or GIL-releasing native code."],
    keys: ["Threads are schedulable entities", "Threads share process resources", "Concurrency and parallelism differ", "Threading has workload-dependent overhead"],
    apis: ["threading.Thread", "win32api.GetCurrentThreadId", "win32process.GetCurrentProcess", "win32process.GetThreadTimes"],
    practice: { title: "See one process, many workers", time: "20 min", intro: "Connect Python worker names to Windows thread IDs and observed I/O.", steps: ["Create three Python worker threads that print threading.get_native_id and pause.", "Find the process in Process Explorer and match each native ID in the Threads tab.", "Have each worker read a different file and capture the process in Process Monitor.", "Group events by TID and compare their timestamps.", "Explain which state is shared and which belongs to each worker."] },
    check: ["What does Windows schedule directly?", ["Executable files", "Threads", "Handle tables", "Virtual address spaces"], 1, "The thread is the basic schedulable entity. Process-level statistics aggregate work done by its threads."],
    sources: [["Processes and threads", "https://learn.microsoft.com/windows/win32/procthread/processes-and-threads"]]
  },
  {
    id: "thread-context-states", module: "threads-scheduling", title: "Thread contexts, stacks, and states",
    lead: "Follow the data Windows must preserve so a thread can stop on one processor and later resume correctly.",
    core: ["A thread context includes instruction pointer, stack pointer, general registers, flags, and architecture-specific state. Saving and restoring this state makes preemption and migration possible.", "The user stack stores call frames for user-mode execution. Kernel transitions use a kernel stack so privileged code does not trust a user-controlled stack. Deep recursion, large local allocations, or runaway thread counts can exhaust stack commitments."],
    mechanics: ["A thread commonly moves among running, ready, waiting, and terminated conditions, with additional internal transition and deferred-ready states. A waiting thread names a condition or dispatcher object; a ready thread is eligible but has not yet been selected.", "Suspension is different from waiting. A suspended thread is prevented from running by a suspend count and may hold locks, so arbitrary suspension is unsafe as a coordination method."],
    windows: ["Wait reasons explain why a thread is blocked, such as waiting for executive, user request, delay, or I/O completion. Start addresses help associate a thread with a module, but injected or dynamically generated code may not map cleanly to a signed image.", "Getting or setting another thread's context requires a suitable handle and careful suspension. It is primarily a debugger and diagnostics technique, not ordinary application control."],
    keys: ["A context makes execution resumable", "User and kernel stacks serve different modes", "Ready differs from waiting", "Suspension is not synchronization"],
    apis: ["OpenThread", "GetThreadContext", "SuspendThread", "ResumeThread", "WaitForSingleObject"],
    practice: { title: "Classify live thread states", time: "15 min", intro: "Use a controlled script with CPU, sleep, and event-wait workers.", steps: ["Start one busy worker, one time.sleep worker, and one worker waiting on an Event.", "Inspect the Threads tab several times and record CPU use, start address, and available state or wait information.", "Signal the event and observe the waiting worker become runnable and finish.", "Explain why the sleeping and event-waiting threads consume little CPU.", "Do not suspend arbitrary system threads during this lab."] },
    check: ["Which thread is eligible to be selected by the scheduler?", ["A ready thread", "A terminated thread", "A waiting thread whose condition is false", "A permanently suspended thread"], 0, "Ready means the thread can run as soon as Windows dispatches it to a suitable processor."],
    sources: [["Thread states", "https://learn.microsoft.com/windows-hardware/drivers/debugger/changing-contexts"]]
  },
  {
    id: "create-end-threads", module: "threads-scheduling", title: "Creating and ending threads",
    lead: "Create workers with clear ownership, join them before cleanup, and understand why forced termination is dangerous.",
    core: ["Creating a thread reserves stack address space, commits initial stack pages, constructs a thread object, and schedules a start routine with one argument. A Python Thread wraps this native lifecycle and provides start and join operations.", "A thread should normally return from its entry function. Process shutdown ends all threads, while TerminateThread stops one without running finally blocks, releasing user-mode locks, or restoring invariants."],
    mechanics: ["Join is a wait on completion, not a request to stop. Cooperative cancellation uses shared state or a waitable event that the worker checks at safe points. The owner signals cancellation, waits for workers, then releases shared resources.", "A thread handle is waitable even after execution ends because the kernel thread object remains while references exist. Close the handle when the caller no longer needs to wait or query it."],
    windows: ["CreateThread is a raw Win32 entry point. Language runtimes may require their own thread creation path so per-thread runtime state is initialized. In Python, prefer threading.Thread unless the lesson specifically studies native calls.", "A process with N worker threads often has at least N plus one total threads, and runtimes or libraries may create more. Measure rather than assuming an exact count."],
    keys: ["Join waits, it does not cancel", "Cooperative shutdown preserves invariants", "Forced termination skips cleanup", "Ended thread objects can remain while handles exist"],
    apis: ["threading.Thread.start", "threading.Thread.join", "CreateThread", "ExitThread", "GetExitCodeThread"],
    practice: { title: "Design a clean worker shutdown", time: "20 min", intro: "Replace an unsafe terminate idea with an event-driven lifecycle.", steps: ["Start three workers that wait with a short timeout on a shared stop event.", "Have each worker perform one small unit, then check cancellation again.", "Signal cancellation in main after a fixed duration.", "Join every worker before closing files or handles they share.", "Inject an exception into one worker function and verify its finally cleanup runs."] },
    check: ["Why is TerminateThread unsafe?", ["It always terminates the whole process", "It can stop code while locks and invariants are unfinished", "It changes the thread ID", "It waits too long"], 1, "Forced termination can interrupt at an arbitrary instruction and bypass language and application cleanup."],
    sources: [["Creating threads", "https://learn.microsoft.com/windows/win32/procthread/creating-threads"]]
  },
  {
    id: "scheduler-dispatch", module: "threads-scheduling", title: "How the scheduler chooses a thread",
    lead: "Model scheduling as a per-processor choice among ready threads, constrained by priority, affinity, processor groups, and current conditions.",
    core: ["Windows uses preemptive, priority-driven scheduling. A ready thread with higher dynamic priority can preempt a lower-priority running thread. Among comparable candidates, time slices and queue order help share processor time.", "Each logical processor can run one scheduled thread at an instant. A machine with two packages and four cores each has eight physical cores, while simultaneous multithreading may expose more logical processors. Real instruction throughput is not simply one instruction per core."],
    mechanics: ["Affinity limits where a thread or process may run. Ideal processor and CPU-set policies express preferences or constraints. Binding too tightly can reduce the scheduler's ability to balance work and exploit idle processors.", "A context switch has direct save/restore costs and indirect cache and translation costs. More threads help only until useful overlap or parallel work is outweighed by those costs and contention."],
    windows: ["The better_together practice demonstrates workload size. A small directory loses to thread startup and coordination; a large I/O workload can benefit from overlap. The thread_overdose practice demonstrates the knee where more workers stop helping.", "Measure wall time, CPU, context-switch delta, worker TIDs, I/O rate, and memory together. One faster run is not enough; warm caches and background load can dominate."],
    keys: ["Scheduling is priority-driven and preemptive", "Each logical processor selects a runnable thread", "Affinity constrains placement", "Optimal worker count depends on the workload"],
    apis: ["win32process.GetProcessAffinityMask", "win32process.SetProcessAffinityMask", "GetActiveProcessorCount", "QueryThreadCycleTime"],
    practice: { title: "Find the workload's useful thread count", time: "35 min", intro: "Reproduce the repository's thread-overdose curve and explain the result with evidence.", steps: ["Run the same file-processing workload with 1, 2, 5, 10, 50, 100, and 500 workers.", "Repeat each count at least three times and record median wall time.", "Capture context-switch and thread-count evidence in Process Explorer or Performance Monitor.", "Plot workers against time and identify the useful region and regression point.", "Explain overhead, I/O queueing, cache effects, and why results differ across machines."] },
    check: ["Why can 500 workers be slower than 100?", ["Windows supports at most 100 threads", "Extra scheduling, stacks, contention, and cache pressure can exceed useful overlap", "The CPU changes to 32-bit mode", "Every worker gets its own process"], 1, "Thread count is not free. Beyond the useful concurrency level, overhead and contention grow while the workload does not."],
    sources: [["Scheduling priorities", "https://learn.microsoft.com/windows/win32/procthread/scheduling-priorities"]]
  },
  {
    id: "priorities-boosts-starvation", module: "threads-scheduling", title: "Priorities, boosts, and starvation",
    lead: "Use priorities to understand responsiveness, not as a generic performance slider.",
    core: ["Windows derives a thread's base priority from its process priority class and thread priority level. Dynamic priority can temporarily change within the variable-priority range. Higher-priority ready threads run before lower-priority ready threads.", "Priority boosts improve responsiveness after waits and help some foreground or I/O-completing work. Windows later decays the dynamic priority toward the base. Real-time priority classes can starve critical system work and should not be used casually."],
    mechanics: ["Priority changes order, not capacity. Raising every worker achieves little because their relative order stays similar, while lower-priority unrelated work may suffer. A CPU-bound high-priority thread that never waits can starve lower-priority threads.", "Priority inversion occurs when a high-priority thread needs a resource held by a lower-priority thread. Good lock design, short critical sections, and OS mechanisms reduce the problem; simply adding more high-priority workers can worsen it."],
    windows: ["Process Explorer can display base priority and let an administrator change process priority class for a controlled experiment. Observe impact with a disposable CPU workload, not a system process.", "SetPriorityClass and SetThreadPriority require suitable handles. Always retain the previous value and restore it in finally for an experiment."],
    keys: ["Priority selects among ready work", "Boosts are temporary responsiveness mechanisms", "Real-time priorities can starve the system", "Raising priority does not create CPU capacity"],
    apis: ["win32process.GetPriorityClass", "win32process.SetPriorityClass", "GetThreadPriority", "SetThreadPriority"],
    practice: { title: "Observe priority without harming the system", time: "20 min", intro: "Use two disposable CPU workers and keep all changes below real-time priority.", steps: ["Start two identical CPU-bound processes and record baseline CPU shares.", "Lower one to Below Normal in Process Explorer and observe several intervals.", "Add another foreground workload and note responsiveness.", "Restore the original class before ending the experiment.", "Explain why the result may differ on a machine with idle cores."] },
    check: ["What does raising a thread's priority directly provide?", ["More processor cores", "Earlier selection relative to lower-priority ready threads", "A larger virtual address space", "Automatic lock ownership"], 1, "Priority affects scheduling order. It does not increase the machine's computational capacity."],
    sources: [["Scheduling priorities", "https://learn.microsoft.com/windows/win32/procthread/scheduling-priorities"]]
  },

  {
    id: "binary-hex-addresses", module: "memory", title: "Addresses in binary and hexadecimal",
    lead: "Read addresses, sizes, masks, and alignment boundaries without treating hexadecimal as decoration.",
    core: ["Memory is byte-addressable, so each address names a byte. Binary shows the bit pattern the hardware uses; hexadecimal compresses four bits into one digit. Address ranges are usually half-open: base <= address < base + size.", "Powers of two produce alignment. If a page is 4096 bytes, its size is 0x1000 and page-aligned addresses end with three hex zeroes. The lower 12 address bits are the offset within a 4 KiB page."],
    mechanics: ["Bit masks isolate fields and flags. address & ~(alignment - 1) rounds down when alignment is a power of two; address % page_size gives the offset. Addition must account for pointer width and overflow in native code.", "KiB, MiB, and GiB use powers of two, while storage marketing often uses decimal units. 2^32 bytes is 4 GiB, even though it is commonly called 4 GB in introductory material."],
    windows: ["VMMap and VirtualQueryEx report base addresses and region sizes in hex-friendly layouts. PE structures store relative virtual addresses, while debuggers show virtual addresses. Being able to add image base and RVA is essential later.", "Python integers do not overflow at pointer width, so explicitly reason about the target process architecture when modeling native addresses."],
    keys: ["One hex digit represents four bits", "0x1000 is 4096 bytes", "Alignment follows powers of two", "Address plus size defines a range"],
    apis: ["ctypes.sizeof", "VirtualQueryEx", "GetSystemInfo"],
    practice: { title: "Annotate a memory region", time: "15 min", intro: "Use one VMMap row as an address arithmetic exercise.", steps: ["Choose a region and record base address and size.", "Calculate the first address after the region.", "Find the page offset of an address inside it.", "Convert the final hex byte to binary.", "Verify calculations with a short Python expression and state the process bitness."] },
    check: ["How many low bits form the offset in a 4 KiB page?", ["4", "8", "12", "16"], 2, "4096 is 2^12, so 12 low-order bits select a byte inside the page."],
    sources: [["Virtual address spaces", "https://learn.microsoft.com/windows-hardware/drivers/gettingstarted/virtual-address-spaces"]]
  },
  {
    id: "memory-hierarchy-caches", module: "memory", title: "The memory hierarchy and CPU caches",
    lead: "Understand why memory performance depends on locality, caching, and access patterns, not only allocation size.",
    core: ["Registers are fastest and smallest, followed by CPU caches, RAM, and persistent storage. Hardware moves fixed-size cache lines between memory and caches. Spatial locality rewards nearby accesses; temporal locality rewards reusing data soon.", "A cache miss stalls or overlaps with other work while data arrives from a slower level. Sequential access is usually friendlier than pointer chasing. Multiple cores maintain coherent caches, but sharing a frequently written cache line can create contention."],
    mechanics: ["The OS manages pages and physical memory, while the CPU caches operate mostly in hardware. The file-system cache uses RAM to retain file data and is distinct from CPU caches. FlushFileBuffers requests durability through the storage stack; it is not a CPU-cache flush.", "Working set, standby lists, and mapped-file pages explain why a second run may be faster. Benchmarking must distinguish cold and warm caches and avoid concluding that fewer observed disk operations means less logical I/O."],
    windows: ["RAMMap shows physical page use and file cache. VMMap shows a process's virtual regions. Process Monitor shows logical I/O requests, not every hardware transaction. Together they separate virtual mapping, RAM residency, and file operations.", "The good and bad logger practice demonstrates buffering. Keeping a handle open reduces create/close work, and avoiding a flush after every record lets Windows batch storage writes."],
    keys: ["Locality drives cache effectiveness", "CPU cache and file cache differ", "Warm-cache benchmarks can mislead", "Flushing trades performance for durability"],
    apis: ["win32file.FlushFileBuffers", "GetProcessMemoryInfo", "QueryWorkingSetEx"],
    practice: { title: "Compare cold and warm reads", time: "20 min", intro: "Measure carefully without claiming more than the tools show.", steps: ["Read the same large test file twice and record elapsed time.", "Observe the file in RAMMap's File Summary before and after.", "Capture logical ReadFile operations in Process Monitor.", "Explain why the second run can be faster even if application code is unchanged.", "State why clearing all caches is intrusive and unnecessary for this conceptual comparison."] },
    check: ["What does FlushFileBuffers primarily request?", ["A CPU register reset", "Buffered file data be sent through the storage stack", "A larger process heap", "A new virtual address"], 1, "It requests that buffered information for the file be flushed toward the device, which can be much more expensive than normal cached writes."],
    sources: [["Cache manager", "https://learn.microsoft.com/windows-hardware/drivers/kernel/windows-kernel-mode-cache-manager"]]
  },
  {
    id: "virtual-address-translation", module: "memory", title: "Virtual memory and address translation",
    lead: "A pointer names a virtual address in one process, and hardware plus Windows translate it to current backing.",
    core: ["Each process sees a private virtual address space. Virtual addresses let code use a stable layout while the Memory Manager maps pages to physical frames, shared file data, or no current resident frame. Isolation comes from separate translation structures and permissions.", "The CPU's memory-management unit walks page tables on a translation miss and caches recent translations in the TLB. Windows changes mappings as pages are committed, protected, shared, paged, and released."],
    mechanics: ["Reserving address space chooses a range without guaranteeing physical storage. Committing creates a promise that pages can be backed by RAM or the paging files when touched. Residency means a committed page is currently present in physical memory.", "A 64-bit process has a much larger user virtual address space than a 32-bit process, but virtual size is not RAM use. Sparse reservations can cover enormous ranges with little committed memory."],
    windows: ["VMMap separates reserved, committed, private, image, mapped-file, heap, and stack regions. Process Explorer distinguishes Private Bytes, Virtual Size, and Working Set. Use the definitions rather than treating all three as memory use.", "VirtualQueryEx reports contiguous regions with common state, protection, and type. It is the foundation of the vmem_to_csv practice."],
    keys: ["Pointers are process-relative virtual addresses", "Reserve, commit, and resident are distinct", "Page tables enforce mappings and protection", "Virtual size is not physical RAM"],
    apis: ["VirtualAlloc", "VirtualFree", "VirtualQueryEx", "win32process.GetProcessMemoryInfo"],
    practice: { title: "Explain three memory numbers", time: "20 min", intro: "Use one process to reconcile virtual size, private bytes, and working set.", steps: ["Record all three values in Process Explorer.", "Reserve a large region without touching it and record the change.", "Commit and touch pages progressively, recording changes each time.", "Use VMMap to identify the new private region.", "Explain which measurement represents address range, committed private backing, and current residency."] },
    check: ["What does reserving virtual memory guarantee?", ["Every page is in RAM", "An address range is set aside", "The file cache is emptied", "A process handle is inherited"], 1, "Reservation protects a virtual range from other allocations. Commitment and residency are separate states."],
    sources: [["Virtual memory", "https://learn.microsoft.com/windows/win32/memory/virtual-memory"]]
  },
  {
    id: "pages-frames-page-tables", module: "memory", title: "Pages, frames, and page tables",
    lead: "Connect fixed-size virtual pages to physical frames and the page-table entries that describe their state and permissions.",
    core: ["A virtual page is a fixed-size slice of virtual address space. A page frame is a same-sized unit of physical memory. A page-table entry records where a page is backed and carries flags such as present, writable, executable, accessed, and dirty.", "Page tables are hierarchical so Windows does not allocate one enormous flat table for every possible address. Large pages reduce translation overhead but require alignment, privileges, and different memory tradeoffs."],
    mechanics: ["The TLB caches translations. A context change, protection update, or mapping change may invalidate entries. Address-space identifiers and targeted invalidation reduce the need for a complete flush on modern CPUs.", "Page protection is enforced during translation. Read, write, execute, guard, and copy-on-write policies can cause faults that the kernel resolves or reports to the process."],
    windows: ["GetSystemInfo reports page size and allocation granularity. These are not the same: Windows commonly uses 4 KiB pages but 64 KiB allocation granularity for reservation addresses.", "RAMMap's Use Counts and Physical Pages views connect frames to use. VMMap stays process-centric. No ordinary user-mode tool shows a simple permanent one-to-one address-to-frame mapping because mappings and residency change."],
    keys: ["Pages are virtual, frames are physical", "Page-table entries store mapping and permission state", "TLBs cache translations", "Page size and allocation granularity differ"],
    apis: ["GetSystemInfo", "VirtualProtect", "QueryWorkingSetEx"],
    practice: { title: "Measure Windows memory units", time: "15 min", intro: "Ask Windows for its page and allocation dimensions, then verify them in a region map.", steps: ["Call GetSystemInfo through ctypes and print dwPageSize and dwAllocationGranularity.", "Reserve several oddly requested allocation sizes.", "Use VirtualQueryEx to record the actual bases and region sizes.", "Explain page rounding and reservation alignment.", "Identify which low address bits represent a 4 KiB page offset."] },
    check: ["What is a page frame?", ["A unit of physical memory", "A thread stack", "A file handle", "A CPU instruction"], 0, "Virtual pages can be mapped to physical page frames while resident."],
    sources: [["System information", "https://learn.microsoft.com/windows/win32/api/sysinfoapi/ns-sysinfoapi-system_info"]]
  },
  {
    id: "page-faults-pagefile", module: "memory", title: "Page faults, paging, and the page file",
    lead: "A page fault is a request for the Memory Manager, not automatically a disk access or an error.",
    core: ["The CPU raises a page fault when a translation cannot satisfy an access. Windows may map a zero-filled page, make a copy-on-write page private, bring data from a standby list, read from an image or mapped file, read from a paging file, grow a guarded stack, or report an access violation.", "A soft fault can be resolved without storage I/O because the page is already in RAM or can be created immediately. A hard fault requires storage access. Fault counts alone do not prove paging-file pressure."],
    mechanics: ["Commit charge is backed by RAM plus paging files in aggregate. The page file supports committed private memory and crash dumps; it is not simply overflow RAM. Windows balances working sets and page lists based on demand and policy.", "Modified pages must be written to appropriate backing before reuse. Clean file-backed pages can often be discarded and reread. The standby list keeps useful cached pages available until another demand needs the frames."],
    windows: ["RAMMap shows active, standby, modified, and free page lists. Process Explorer shows page-fault deltas, but use performance counters and workload timing to decide whether faults are harmful.", "The alligator practice allocates memory progressively. Observe commit and working set before, during, and after touches rather than assuming allocation immediately consumes equivalent RAM."],
    keys: ["Page faults can be normal", "Soft faults need no storage read", "Commit differs from residency", "The page file backs committed private memory"],
    apis: ["GlobalMemoryStatusEx", "GetProcessMemoryInfo", "VirtualLock", "EmptyWorkingSet"],
    practice: { title: "Observe allocation and first touch", time: "25 min", intro: "Use alligator.py to separate allocation, commitment, touching, and residency.", steps: ["Record system commit and the Python process's private bytes and working set.", "Run alligator.py with a modest, safe allocation size.", "Record metrics after allocation and after every page is touched.", "Observe page-fault deltas and VMMap region type.", "Release the allocation and explain which metrics fall immediately and which system caches may retain pages."] },
    check: ["Which statement about a page fault is correct?", ["It always reads the page file", "It always crashes the process", "Windows may resolve it without storage I/O", "It occurs only in kernel mode"], 2, "Demand-zero, transition, copy-on-write, and other soft faults can be satisfied without a storage read."],
    sources: [["Page files", "https://learn.microsoft.com/windows/troubleshoot/windows-client/performance/introduction-to-the-page-file"]]
  },
  {
    id: "shared-memory-copy-on-write", module: "memory", title: "Shared memory and copy-on-write",
    lead: "Windows can map the same backing into several processes while preserving isolation when a writer needs a private copy.",
    core: ["Image files and mapped data can be shared at page granularity. Separate processes use different virtual addresses that refer to the same physical frames while protections and backing permit sharing.", "Copy-on-write mappings begin shared and readable. On a write, a page fault lets Windows allocate a private frame, copy the contents, update that process's mapping, and resume the instruction. Other processes keep their original view."],
    mechanics: ["A file-mapping section object represents the shared backing. CreateFileMapping can use a file handle or paging-file backing; MapViewOfFile maps a view into a process. Names or duplicated/inherited handles let another process access the same section.", "Shared memory moves bytes but does not define message boundaries or coordination. Processes need a protocol and synchronization to avoid observing partially updated state."],
    windows: ["VMMap distinguishes image, mapped file, shareable, and private memory. The same DLL image may be mapped in many processes, while writable private pages account separately after modification.", "Use Local or Global object naming intentionally and supply a security descriptor when trust boundaries matter. A guessable name is not access control."],
    keys: ["Sharing occurs through common backing", "Copy-on-write preserves isolation on modification", "Mappings need a synchronization protocol", "Names locate objects but do not authorize access"],
    apis: ["CreateFileMappingW", "OpenFileMappingW", "MapViewOfFile", "UnmapViewOfFile"],
    practice: { title: "Share one structured record", time: "30 min", intro: "Create a pagefile-backed mapping used by a writer and reader process.", steps: ["Define a fixed binary layout with version, sequence, length, and payload fields.", "Create a named mapping and map a writable view in the producer.", "Open and map a read-only view in the consumer.", "Add an event so the consumer reads only after the producer completes an update.", "Unmap views and close mapping and event handles in both processes."] },
    check: ["What happens when a process writes a copy-on-write page?", ["Every process sees the write", "Windows gives that process a private copy", "The process exits", "The page becomes a handle"], 1, "The fault handler copies the page and remaps the writer to a private frame."],
    sources: [["File mapping", "https://learn.microsoft.com/windows/win32/memory/file-mapping"]]
  },
  {
    id: "virtualalloc-heaps-protection", module: "memory", title: "VirtualAlloc, protection, and heaps",
    lead: "Choose between address-space operations and heap allocation, then make state, protection, and cleanup explicit.",
    core: ["VirtualAlloc reserves and commits page-granular regions. VirtualFree decommits or releases them under specific parameter rules. VirtualProtect changes protection on committed pages and returns the previous protection so callers can restore it.", "Heaps suballocate smaller blocks from larger regions and maintain metadata. Most application allocation goes through language or process heaps. Direct virtual allocation is appropriate for large regions, exact protection, guard pages, executable-code scenarios, or memory inspection exercises."],
    mechanics: ["Writable and executable at the same time increases risk. Just-in-time compilers typically write data, change the region to executable, and flush the instruction cache. Data buffers should remain non-executable.", "VirtualQueryEx walks regions by adding RegionSize to BaseAddress. Correct ctypes structure layout must use pointer-sized fields, and the loop must stop safely at the target architecture's address limit or on a zero return."],
    windows: ["The vmem_to_csv practice combines OpenProcess, VirtualQueryEx, MEMORY_BASIC_INFORMATION, state/protection decoding, and CSV export. It must request only query rights and close the process handle.", "Compare the exported rows with VMMap. Differences can arise because the process changes between snapshots and because VMMap applies richer categorization than raw VirtualQueryEx fields."],
    keys: ["VirtualAlloc manages regions while heaps manage blocks", "Protection is independent of commitment state", "Avoid writable-executable memory", "VirtualQueryEx returns contiguous regions"],
    apis: ["VirtualAlloc", "VirtualFree", "VirtualProtect", "VirtualQueryEx", "HeapAlloc", "HeapFree"],
    practice: { title: "Export and reconcile a virtual memory map", time: "40 min", intro: "Complete vmem_to_csv.py as a disciplined ctypes and memory-model exercise.", steps: ["Define MEMORY_BASIC_INFORMATION with architecture-correct ctypes fields.", "Configure VirtualQueryEx argtypes and restype, then open the target with PROCESS_QUERY_INFORMATION.", "Walk by BaseAddress plus RegionSize and decode state, type, and protection flags.", "Write stable CSV columns and close the process handle in finally.", "Compare total categories with VMMap and explain snapshot and classification differences."] },
    check: ["What should a VirtualQueryEx region walker add for its next address?", ["One byte", "The current region's BaseAddress plus RegionSize", "The process ID", "The page-file size"], 1, "Each result describes a contiguous region. Advancing to its end avoids redundant queries and infinite loops."],
    sources: [["VirtualAlloc", "https://learn.microsoft.com/windows/win32/api/memoryapi/nf-memoryapi-virtualalloc"], ["VirtualQueryEx", "https://learn.microsoft.com/windows/win32/api/memoryapi/nf-memoryapi-virtualqueryex"]]
  },

  {
    id: "compile-link-execute", module: "linking-loading", title: "From source code to an executable",
    lead: "Follow source through preprocessing, compilation, object files, linking, PE output, mapping, and entry-point execution.",
    core: ["A compiler translates source units into object files containing machine code, data, symbols, relocations, and metadata. A linker combines those objects and libraries, resolves symbols it can know, lays out sections, and writes a Portable Executable image.", "The PE on disk is not copied blindly into RAM. Headers describe how sections should be mapped, their virtual sizes and protections, entry point, imports, resources, relocations, and architecture."],
    mechanics: ["Link-time errors mean a required symbol could not be resolved while building. Load-time errors mean the image, a dependency, or an imported export could not be satisfied when starting. Run-time failures happen after execution begins.", "Managed .NET assemblies also use PE containers but add CLR metadata and intermediate language. Identify the image type before applying native-code assumptions."],
    windows: ["CFF Explorer or another PE viewer exposes headers and directories. Sigcheck reports architecture, version, signature, and hashes without changing the file. Process Explorer shows the mapped image and modules after loading.", "Architecture must match where required. A 32-bit process cannot directly load a 64-bit DLL, and the PE Machine and Optional Header Magic fields help establish compatibility."],
    keys: ["Object files precede the final executable", "The linker resolves and lays out symbols", "PE describes disk and memory layout", "Architecture compatibility matters"],
    apis: ["GetBinaryTypeW", "GetModuleFileNameW", "CreateProcessW"],
    practice: { title: "Trace one binary through its representations", time: "20 min", intro: "Use a small native executable and compare file metadata with its live process.", steps: ["Inspect Machine, section count, entry-point RVA, and imports in a PE viewer.", "Run Sigcheck and record architecture, hash, and signature status.", "Start the program and find its image base and loaded modules in Process Explorer.", "Calculate image base plus entry-point RVA.", "Explain why the file offset is not generally the same as the virtual address."] },
    check: ["What is the linker's main role?", ["Schedule threads", "Combine objects and resolve symbols into an image", "Perform access checks", "Translate virtual pages"], 1, "The linker produces the final image layout and resolves references available at build time."],
    sources: [["PE format", "https://learn.microsoft.com/windows/win32/debug/pe-format"]]
  },
  {
    id: "static-dynamic-linking", module: "linking-loading", title: "Static and dynamic linking",
    lead: "Decide whether library code becomes part of the image or remains a dependency resolved by the Windows loader.",
    core: ["Static linking copies needed library code into the output image. Deployment can be simpler, but each executable carries its own copy and library fixes require relinking. The linker resolves those references at build time.", "Dynamic linking keeps code in a DLL. Multiple processes can map shared image pages, applications can receive library fixes, and optional features can load on demand. Deployment, versioning, architecture, and search-path behavior become part of correctness and security."],
    mechanics: ["Implicit dynamic linking records imports in the PE and lets the loader resolve them during startup. Explicit loading calls LoadLibrary and GetProcAddress at runtime, which supports optional components and version checks but shifts error handling and signature knowledge to the code.", "A DLL load increments a per-process reference count. Each successful LoadLibrary should be matched with FreeLibrary when the reference is no longer needed. A module already present can be queried with GetModuleHandle without necessarily owning a new reference."],
    windows: ["ListDLLs and Process Explorer show loaded modules, paths, versions, and signatures. Process Monitor Image Load events reveal timing and selected path. A module name alone is insufficient because search order may select an unintended file.", "Modern secure code uses well-defined paths and safe DLL search configuration rather than relying on the current directory."],
    keys: ["Static code is incorporated at link time", "Dynamic imports are loader-resolved", "Explicit loading needs manual error and lifetime handling", "DLL search paths are security-sensitive"],
    apis: ["win32api.LoadLibrary", "win32api.GetProcAddress", "win32api.FreeLibrary", "SetDefaultDllDirectories"],
    practice: { title: "Compare implicit and explicit dependencies", time: "25 min", intro: "Observe a program's startup imports and one DLL it loads only after user action.", steps: ["Inspect the executable's import directory before running it.", "Capture Image Load events for the program.", "Trigger an optional feature and find the late-loaded module.", "Use ListDLLs to confirm the final module path and signature.", "Classify each dependency as implicit, transitive, or explicitly loaded based on evidence."] },
    check: ["What must follow a successful LoadLibrary when the reference is no longer needed?", ["CloseHandle", "FreeLibrary", "VirtualFree", "RegCloseKey"], 1, "LoadLibrary adds a module reference. FreeLibrary releases that matching reference."],
    sources: [["Dynamic-link libraries", "https://learn.microsoft.com/windows/win32/dlls/dynamic-link-libraries"]]
  },
  {
    id: "pe-anatomy", module: "linking-loading", title: "Anatomy of a Portable Executable",
    lead: "Read the headers that tell Windows what an image is, where its parts belong, and which supporting tables exist.",
    core: ["A PE begins with the DOS MZ header. The e_lfanew field at offset 0x3C points to the PE signature and NT headers. The COFF File Header records Machine, NumberOfSections, SizeOfOptionalHeader, and Characteristics.", "The so-called Optional Header is required for images. It includes Magic, AddressOfEntryPoint, ImageBase, alignments, image size, subsystem, and data-directory entries. PE32 and PE32+ layouts differ."],
    mechanics: ["Data directories are RVA and size pairs for structures such as imports, exports, resources, relocations, exception data, TLS, and security. A zero entry means that directory is absent. The certificate table is unusual because its address is a file offset, not an RVA.", "The section table follows the optional header. A robust parser validates sizes, bounds, integer arithmetic, and architecture before following offsets. Treat malformed files as hostile input."],
    windows: ["CFF Explorer makes fields visual, while Python's struct module can parse a few fields for learning. Do not execute or modify an unknown sample merely to inspect it.", "The how_to_load practice established architecture compatibility with Machine 0x014C and Optional Header Magic 0x010B, both indicating a 32-bit x86 image context."],
    keys: ["MZ and PE signatures locate the header chain", "Machine and Magic establish architecture and layout", "Data directories point to major tables", "Parsers must validate file bounds"],
    apis: ["ImageNtHeader", "MapAndLoad", "GetBinaryTypeW"],
    practice: { title: "Parse four PE facts safely", time: "25 min", intro: "Read metadata without loading the image as executable code.", steps: ["Open a known local executable in binary mode.", "Validate MZ, read e_lfanew, and validate PE followed by two zero bytes.", "Read Machine and NumberOfSections from the File Header.", "Read Optional Header Magic and AddressOfEntryPoint.", "Compare the values with CFF Explorer and reject files whose offsets exceed file size."] },
    check: ["What does e_lfanew identify?", ["The process ID", "The file offset of the PE headers", "The preferred image base", "The first imported DLL"], 1, "The DOS header's e_lfanew field points to the PE signature and NT headers."],
    sources: [["PE format", "https://learn.microsoft.com/windows/win32/debug/pe-format"]]
  },
  {
    id: "sections-rvas", module: "linking-loading", title: "Sections and relative virtual addresses",
    lead: "Translate between raw file layout, mapped section layout, relative virtual addresses, and live virtual addresses.",
    core: ["Sections group content with related use and protection. Typical names include .text for executable code, .rdata for read-only data, .data for writable globals, and .rsrc for resources, but names are conventions rather than proof.", "PointerToRawData and SizeOfRawData describe file placement. VirtualAddress and VirtualSize describe mapped placement relative to the image base. SectionAlignment and FileAlignment explain padding and why the layouts differ."],
    mechanics: ["An RVA is an offset from the loaded image base. VA = image base + RVA. To locate an RVA in the file, find the section whose virtual range contains it, then use PointerToRawData + (RVA - section VirtualAddress), with careful rules for headers and zero-filled tails.", "ASLR can choose a base different from the preferred ImageBase. Relocation records let the loader adjust address-dependent values when the image is not loaded at its preferred address."],
    windows: ["Process Explorer reports module base addresses. A PE viewer reports RVAs. Combining them lets you calculate a live entry-point or export address without assuming a fixed base.", "Section characteristics become page protections during mapping, although pages and sections do not align one-to-one in every edge case. Writable-executable sections deserve investigation."],
    keys: ["RVA is relative to the loaded base", "Raw and virtual layouts differ", "ASLR moves image bases", "Section flags guide mapped protections"],
    apis: ["GetModuleHandleW", "VirtualQueryEx", "EnumProcessModules"],
    practice: { title: "Resolve RVA to file and memory", time: "20 min", intro: "Calculate the same byte's two locations.", steps: ["Choose an RVA inside .text from a known PE.", "Use the section header to calculate its file offset.", "Read the process's live module base and calculate the VA.", "Confirm the VA falls inside an executable image region in VMMap.", "Restart the process and observe whether ASLR changes the base while the RVA stays constant."] },
    check: ["How is a live virtual address normally calculated from an RVA?", ["RVA minus file size", "Image base plus RVA", "PID plus RVA", "Raw offset plus page-file size"], 1, "An RVA is defined relative to the image's loaded base."],
    sources: [["PE format sections", "https://learn.microsoft.com/windows/win32/debug/pe-format#section-table-section-headers"]]
  },
  {
    id: "imports-exports-iat", module: "linking-loading", title: "Imports, exports, and the IAT",
    lead: "See how one image names external functions and how the loader replaces those names with callable addresses.",
    core: ["The import directory groups dependencies by DLL. Import lookup data names functions by string or ordinal. During loading, Windows locates each exporting module and function, then writes the resolved virtual address into the Import Address Table.", "An export directory maps names or ordinals to RVAs, sometimes forwarding an export to another DLL. A function name in a header, exported symbol name, and pywin32 wrapper name can differ."],
    mechanics: ["An IAT call is indirect: code reads the current function address from a table slot. This supports relocation and shared libraries. It also makes IAT hooking possible by replacing the slot, but the replacement must preserve calling convention, parameters, return type, thread safety, and original flow.", "GetProcAddress performs a similar lookup for explicit loading. It returns an untyped address; ctypes code must construct the exact callable signature before use."],
    windows: ["The open_the_box practice loads User32, resolves MessageBoxA, defines a callable prototype, and calls it. It deliberately exposes the type work hidden by a normal import.", "CFF Explorer displays imports and exports. Process Monitor shows image loads. Process Explorer or ListDLLs confirms which physical module supplied the code."],
    keys: ["Imports describe external requirements", "Exports publish names or ordinals", "The IAT holds resolved addresses", "A raw function address needs an exact signature"],
    apis: ["win32api.LoadLibrary", "win32api.GetProcAddress", "ctypes.WINFUNCTYPE", "FreeLibrary"],
    practice: { title: "Open the box safely", time: "35 min", intro: "Complete open_the_box.py and explain each ABI decision.", steps: ["Configure LoadLibraryA and GetProcAddress with explicit argtypes and restype.", "Load User32.dll and resolve MessageBoxA.", "Create a WINFUNCTYPE matching HWND, LPCSTR, LPCSTR, UINT, and int return.", "Call it with byte strings, inspect the result, and release the library reference.", "Repeat the design on paper for MessageBoxW and identify every type that changes."] },
    check: ["What does GetProcAddress return?", ["A fully typed Python function", "An address that must be called with the correct native signature", "A process handle", "An import descriptor"], 1, "The address has no Python type information. Calling it with the wrong ABI or types is unsafe."],
    sources: [["GetProcAddress", "https://learn.microsoft.com/windows/win32/api/libloaderapi/nf-libloaderapi-getprocaddress"]]
  },
  {
    id: "windows-loader", module: "linking-loading", title: "How the Windows loader loads a process",
    lead: "Trace the user-mode loader as it maps dependencies, resolves imports, applies relocations, initializes TLS, and reaches the entry point.",
    core: ["Process creation maps the main image and Ntdll. The user-mode loader initializes the process, maps dependent DLLs recursively, resolves imports, applies base relocations when needed, establishes loader data, processes TLS, calls DLL initialization routines, and finally transfers control to the executable entry point.", "Dependencies form a graph, not a flat list. The loader avoids loading the same module repeatedly within one process and maintains reference and initialization state. Cycles and loader-lock reentrancy make complex DllMain behavior dangerous."],
    mechanics: ["DllMain runs under loader constraints. It should do minimal work and avoid operations that may load libraries, start complex synchronization, or wait on other threads. Defer real initialization to an explicit function after loading.", "Search order determines which DLL path is selected. Use application packaging, absolute paths where appropriate, AddDllDirectory, SetDefaultDllDirectories, and safe deployment practices rather than current-directory assumptions."],
    windows: ["The how_to_load exercise edits a disposable 32-bit executable's import table so a matching 32-bit DLL loads at startup. This demonstrates loader behavior, but modifying production executables breaks signatures and creates security risk.", "Capture Image Load events and compare them with the static import directory. Transitive dependencies and explicitly loaded modules explain why the live list is longer."],
    keys: ["Loader work is mostly user mode", "Dependencies load recursively", "DllMain must remain minimal", "DLL selection depends on secure search behavior"],
    apis: ["LoadLibraryExW", "AddDllDirectory", "SetDefaultDllDirectories", "GetModuleFileNameW"],
    practice: { title: "Prove a startup dependency", time: "35 min", intro: "Use only matching, disposable lab binaries in an isolated directory or VM.", steps: ["Confirm executable and DLL Machine and Optional Header Magic values match.", "Record the original hash, signature state, and import directory.", "Add the lab DLL import and rebuild the disposable executable.", "Verify the new import entry before execution, then run under an Image Load filter.", "Confirm the loaded path, explain why DllMain ran, and restore or discard the modified lab copy."] },
    check: ["Why should DllMain avoid complex work?", ["It runs before the PE exists", "It executes under loader constraints and can deadlock through reentrant loading or waits", "It cannot access memory", "It always runs in kernel mode"], 1, "Loader initialization holds internal state and constraints. Reentrant loader activity or cross-thread waits can deadlock the process."],
    sources: [["Dynamic-link library best practices", "https://learn.microsoft.com/windows/win32/dlls/dynamic-link-library-best-practices"]]
  },
  {
    id: "registry-structure", module: "management", title: "How the Registry is structured",
    lead: "Treat the Registry as a typed, hierarchical configuration database with several views, not as one magic file.",
    core: ["Registry keys resemble directories and values resemble named typed records. A value has a name, type, and data; every key can also have an unnamed default value. Common types include REG_SZ, REG_EXPAND_SZ, REG_DWORD, REG_BINARY, and REG_MULTI_SZ.", "Root-key handles expose views such as HKEY_LOCAL_MACHINE and HKEY_CURRENT_USER. HKCU reflects the current user's profile, HKCR is a merged class-registration view, and several roots are aliases or constructed views rather than independent on-disk databases."],
    mechanics: ["Registry hives persist parts of the hierarchy in files, but applications should use Registry APIs rather than editing hive files. Keys are securable kernel objects, so opening one requests access rights and returns a handle.", "A path normally ends at a key; the value name is passed separately to query or set operations. Confusing the two causes common automation errors. Environment expansion occurs only when the value type and consuming application support it."],
    windows: ["Regedit shows the hierarchy and types. Process Monitor records RegOpenKey, RegQueryValue, RegSetValue, and related activity. Autoruns interprets many startup-related locations but is not a complete Registry browser.", "Changes under system-wide roots can affect every user or boot behavior. Use a disposable key under HKCU\\Software\\ILOVEOSLab for practice and delete only that exact lab key afterward."],
    keys: ["Keys contain subkeys and typed values", "Root keys can be views or aliases", "Registry access is handle-based and secured", "Use a safe HKCU lab path"],
    apis: ["winreg.OpenKey", "winreg.QueryValueEx", "winreg.EnumKey", "winreg.CloseKey"],
    practice: { title: "Map one application's configuration", time: "20 min", intro: "Observe reads before changing anything.", steps: ["Choose a harmless application preference and capture its process in Process Monitor.", "Filter to Registry events and change the preference once.", "Identify the key, value name, type, and data access pattern.", "Confirm the value in Regedit without modifying unrelated entries.", "Explain whether the setting is per-user or machine-wide and what evidence supports that conclusion."] },
    check: ["Which analogy is most accurate?", ["A key is like a directory and a value is like a typed record", "A value is always another key", "Every root is a separate disk", "REG_DWORD is a text encoding"], 0, "Keys form the hierarchy, while named values store typed data inside a key."],
    sources: [["Registry structure", "https://learn.microsoft.com/windows/win32/sysinfo/structure-of-the-registry"]]
  },
  {
    id: "registry-python", module: "management", title: "Reading and changing Registry data",
    lead: "Open the narrowest Registry view and access mask, preserve value types, and make every write reversible.",
    core: ["Python's winreg module wraps the standard Registry APIs. OpenKey takes a root handle, subkey, reserved value, and access mask. QueryValueEx returns both data and type. EnumKey and EnumValue support discovery when names are unknown.", "Writes require explicit access and correct types. SetValueEx receives the value name, reserved value, registry type, and data. Use CreateKeyEx only when creation is intended, because it can also open an existing key."],
    mechanics: ["Registry handles must be closed, either with a context manager or CloseKey. Catch FileNotFoundError for an absent key or value only when absence is expected; do not hide PermissionError or malformed data.", "On 64-bit Windows, KEY_WOW64_32KEY and KEY_WOW64_64KEY select a view where redirection applies. The Python interpreter architecture can influence defaults, so state the intended view in cross-architecture tools."],
    windows: ["A robust change records whether the value existed, its old type, and old data before writing. A finally or explicit restore path can then return the lab to its original state.", "Process Monitor verifies which path and view the program actually accessed. Reg.exe is useful for independent read-only confirmation."],
    keys: ["QueryValueEx returns data and type", "Close key handles", "Writes should be reversible", "Choose 32-bit or 64-bit views intentionally"],
    apis: ["winreg.OpenKey", "winreg.CreateKeyEx", "winreg.QueryValueEx", "winreg.SetValueEx", "winreg.DeleteValue"],
    practice: { title: "Build a reversible Registry probe", time: "30 min", intro: "Work only under HKCU\\Software\\ILOVEOSLab.", steps: ["Create the exact lab key and record whether a DemoMode value already exists.", "Write DemoMode as REG_DWORD, read it back with its type, then enumerate the key.", "Capture the operations in Process Monitor.", "Restore the original value or remove only the value you created.", "Close every key and confirm the lab state in Regedit."] },
    check: ["What does winreg.QueryValueEx return?", ["Only text", "A data and Registry-type pair", "A process handle", "A service status"], 1, "The type is required to interpret and preserve the value correctly."],
    sources: [["Registry functions", "https://learn.microsoft.com/windows/win32/sysinfo/registry-functions"]]
  },
  {
    id: "services-scm", module: "management", title: "Windows services and the SCM",
    lead: "A service is an SCM-managed background program with registered configuration, control handling, status, and security.",
    core: ["The Service Control Manager is implemented by services.exe and maintains the service database. A service record names the binary or host, start type, account, dependencies, and other configuration. Running service state includes current state, accepted controls, exit codes, checkpoints, and wait hints.", "Services may start at boot, on demand, or through triggers. They run without an interactive user desktop and commonly expose IPC endpoints. A service is not merely any background process."],
    mechanics: ["OpenSCManager returns a handle to the SCM database with requested rights. OpenService returns a service handle with rights such as SERVICE_QUERY_STATUS, SERVICE_START, or SERVICE_STOP. StartService requests a start; ControlService sends accepted controls.", "Start and stop are asynchronous transitions. Correct clients query status and use checkpoints and wait hints rather than assuming the requested state is immediate."],
    windows: ["Services.msc, sc.exe, Get-Service, PsService, and Process Explorer show complementary views. Process Monitor can capture process start, Registry configuration reads, file access, and IPC activity during service startup.", "Many operations require elevation and some services cannot or should not be stopped. Practice only with a documented, non-critical service on a VM or a service you created for the lab."],
    keys: ["SCM configuration differs from running state", "Service handles carry specific rights", "State changes are asynchronous", "Use a non-critical lab target"],
    apis: ["win32service.OpenSCManager", "win32service.OpenService", "win32service.QueryServiceStatusEx", "win32service.StartService", "win32service.ControlService"],
    practice: { title: "Explain one service end to end", time: "25 min", intro: "Start with read-only inspection.", steps: ["Choose a non-critical service and record its service name, display name, account, start type, and binary path.", "Find its process and PID if running, noting whether it shares svchost.exe.", "Query status through pywin32 or sc.exe.", "Inspect its service security with AccessChk or sc sdshow.", "Draw SCM record, process, token, dependencies, and any observed IPC endpoint."] },
    check: ["What is the service name used for?", ["It is the stable SCM database identifier", "It is always the process filename", "It is a thread ID", "It is the display-only description"], 0, "The service name is the identifier supplied to APIs such as OpenService; the display name can differ."],
    sources: [["Services", "https://learn.microsoft.com/windows/win32/services/services"]]
  },
  {
    id: "control-services-python", module: "management", title: "Controlling services from Python",
    lead: "Implement both the convenient pywin32 path and the exact ctypes contract used by the repository service-controller practice.",
    core: ["win32service exposes direct SCM operations, while win32serviceutil provides convenient administration helpers. Direct calls make access masks, status values, handles, and cleanup visible. High-level helpers are appropriate when those details are not the subject.", "The ctypes path loads Advapi32.dll with use_last_error=True and declares OpenSCManagerW, OpenServiceW, StartServiceW, ControlService, and CloseServiceHandle. SC_HANDLE is pointer-sized, BOOL uses zero for failure, and SERVICE_STATUS fields must follow the native order."],
    mechanics: ["Request SC_MANAGER_CONNECT from the SCM and only SERVICE_START or SERVICE_STOP from the target service for the selected action. If opening the service fails after the SCM opened, preserve the last error, close the SCM handle, restore the error, and then raise it.", "CloseServiceHandle, not CloseHandle, releases SCM and service handles. Capture errors immediately. Treat already running, not active, access denied, and cannot accept control as distinct expected cases where useful."],
    windows: ["ControlService is implemented by Advapi32.dll. Microsoft Learn lists behavior, parameters, result, and error codes, not C++-style thrown exceptions. Python bindings decide whether a failure becomes an exception.", "Verify state independently with Get-Service or QueryServiceStatusEx and wait through pending states before reporting completion."],
    keys: ["Declare every ctypes signature", "Ask for action-specific access", "Preserve last error across cleanup", "Service handles use CloseServiceHandle"],
    apis: ["win32service.ControlService", "OpenSCManagerW", "OpenServiceW", "StartServiceW", "CloseServiceHandle"],
    practice: { title: "Complete real_service_controller.py", time: "45 min", intro: "Use an elevated terminal and a safe service target.", steps: ["Define SERVICE_STATUS with seven DWORD fields in documented order.", "Set argtypes and restype for all five Advapi32 functions.", "Parse start or stop and request only the needed service right.", "Check null or zero returns immediately and raise WinError with operation context.", "Close service then SCM handles in finally and verify the resulting status independently."] },
    check: ["Which DLL implements ControlService?", ["Kernel32.dll", "Advapi32.dll", "User32.dll", "Ntdll.exe"], 1, "The service-management APIs are exported by Advapi32.dll."],
    sources: [["ControlService", "https://learn.microsoft.com/windows/win32/api/winsvc/nf-winsvc-controlservice"]]
  },
  {
    id: "svchost-background", module: "management", title: "Service hosts and background processes",
    lead: "Distinguish executable services, DLL-based services, shared hosts, service groups, and ordinary background applications.",
    core: ["Some services run as their own executable. DLL-based services need a host process, commonly svchost.exe, that loads the service DLL and connects it to the SCM. Multiple services may share a host, although modern Windows often separates them when resources permit.", "A crash or resource spike in one shared host may affect several services. The PID identifies the host instance, while the service name identifies the SCM-managed service. Correlate both."],
    mechanics: ["The SCM launches the host with a service group and supplies activation information. ServiceDll configuration identifies a DLL for many hosted services. The host registers control handlers and reports state for each service.", "Background does not imply service. Startup apps, scheduled tasks, tray processes, and packaged-app background tasks have different lifecycles and management mechanisms."],
    windows: ["Process Explorer can show services hosted inside each svchost instance. tasklist /svc and sc queryex provide command-line mappings. Autoruns exposes services alongside other automatic-start mechanisms.", "Inspect signatures, paths, command lines, accounts, and service membership together. A familiar filename in an unexpected path is more significant than a familiar name alone."],
    keys: ["One host can contain several services", "Service identity differs from host PID", "Not every background process is a service", "Path and signature add identity evidence"],
    apis: ["EnumServicesStatusExW", "QueryServiceConfigW", "win32service.EnumServicesStatus"],
    practice: { title: "Map a service host", time: "20 min", intro: "Build a service-to-process table using two independent views.", steps: ["Choose one svchost.exe instance in Process Explorer.", "List the service names it hosts and record PID, user, command line, and integrity.", "Confirm the mapping with tasklist /svc or sc queryex.", "Look up each service's display name and binary configuration without changing it.", "Explain the blast radius if that host exits."] },
    check: ["Why can several services share one PID?", ["They are DLL-based services loaded into one host", "PIDs are never unique", "Every service is a thread", "The Registry merges processes"], 0, "Svchost can load multiple DLL-based services into a shared process while SCM tracks each service separately."],
    sources: [["Service programs", "https://learn.microsoft.com/windows/win32/services/service-programs"]]
  },
  {
    id: "wow64-redirection", module: "management", title: "WoW64 and redirection",
    lead: "Understand how 32-bit applications run on 64-bit Windows and why familiar paths can resolve to architecture-specific views.",
    core: ["WoW64 combines processor compatibility support, user-mode translation DLLs, and kernel support so x86 applications can run on x64 Windows. It does not make a 32-bit process able to load 64-bit DLLs.", "For compatibility, selected file-system and Registry accesses use architecture-specific views. Despite the names, System32 contains native 64-bit system binaries on x64 Windows, while SysWOW64 contains 32-bit binaries. Sysnative is a virtual alias 32-bit programs can use to reach native System32."],
    mechanics: ["Registry redirector behavior applies to selected keys, notably software registration views. KEY_WOW64_32KEY and KEY_WOW64_64KEY choose a view explicitly. Some keys are shared, redirected, or reflected depending on Windows version, so rely on current documentation and observation.", "File-system redirection can be disabled temporarily through APIs, but doing so broadly is fragile. Prefer correct explicit paths and architecture-matched components."],
    windows: ["Process Explorer marks 32-bit images and shows loaded module paths. Process Monitor reveals the resolved path after redirection. Compare a 32-bit and 64-bit process accessing the same apparent path or Registry location.", "UAC virtualization is a separate compatibility feature that can redirect writes from legacy unelevated applications. Do not conflate it with WoW64 architecture redirection."],
    keys: ["WoW64 runs x86 applications on x64 Windows", "System32 contains native binaries", "Registry APIs can select a view", "UAC virtualization is separate"],
    apis: ["IsWow64Process2", "Wow64DisableWow64FsRedirection", "KEY_WOW64_32KEY", "KEY_WOW64_64KEY"],
    practice: { title: "Prove a redirected view", time: "25 min", intro: "Use read-only probes from both interpreter architectures if available.", steps: ["Confirm each Python interpreter architecture with pointer size.", "Read the same selected HKLM\\Software location using default, 32-bit, and 64-bit flags.", "Capture Registry operations in Process Monitor.", "Compare system binary resolution from the two processes.", "Explain the observed view without changing system configuration."] },
    check: ["What is stored in System32 on x64 Windows?", ["Only 32-bit binaries", "Native 64-bit system binaries", "Registry hives only", "Python modules only"], 1, "The naming is historical. SysWOW64 contains the 32-bit system components."],
    sources: [["WOW64 implementation details", "https://learn.microsoft.com/windows/win32/winprog64/implementation-details"]]
  },

  {
    id: "security-model", module: "security", title: "The Windows security model",
    lead: "Reason about access as subject, requested action, and protected object, with auditing and mandatory policy layered around the decision.",
    core: ["A subject acts through a thread security context, usually derived from a process token. The object is a securable resource such as a file, process, Registry key, service, or named synchronization object. The requested action is an access mask meaningful to that object type.", "Windows discretionary access control lets an object's owner influence its DACL. The Security Reference Monitor performs kernel access checks. Authentication establishes identities; authorization decides allowed actions; auditing records configured security-relevant events."],
    mechanics: ["Opening an object is the key check point. The caller requests rights, Windows evaluates the token and security descriptor, and a successful handle records granted rights. Later operations check the handle's granted access rather than redoing the original name-based open.", "Privileges and mandatory integrity policy can alter or further restrict the result. Security is therefore more than matching a username to a file permission."],
    windows: ["AccessChk answers what access a principal appears to have to files, Registry keys, services, processes, and other objects. Process Explorer shows tokens and handle access. Event Viewer or win32evtlog can inspect configured audit results.", "Access denied is expected when the requested action, token, object policy, integrity, protection level, or system policy does not permit the operation."],
    keys: ["Authorization evaluates subject, action, and object", "Access is checked when a handle is opened", "Handles preserve granted rights", "Auditing is configured separately from permission"],
    apis: ["AccessCheck", "OpenProcessToken", "GetNamedSecurityInfo", "win32evtlog.ReadEventLog"],
    practice: { title: "Explain one access-denied result", time: "25 min", intro: "Use a harmless read-only query and resist solving it by requesting all access.", steps: ["Choose a process or file your ordinary token cannot fully open.", "Record the exact requested access mask and error code.", "Inspect your token groups, integrity, and enabled privileges.", "Inspect the target security descriptor with an allowed tool.", "Identify the most likely failed policy layer and redesign the request with the minimum right needed."] },
    check: ["When is the main discretionary access check normally performed?", ["When opening the object for requested rights", "Every time the CPU executes an instruction", "Only when a user logs off", "When a PID is allocated"], 0, "A successful open produces a handle whose entry records the rights granted by that check."],
    sources: [["Access control model", "https://learn.microsoft.com/windows/win32/secauthz/access-control-model"]]
  },
  {
    id: "users-groups-sids", module: "security", title: "Users, groups, and SIDs",
    lead: "Use security identifiers as durable identities and account names as human-readable translations.",
    core: ["A SID is a variable-length binary identifier for a user, group, computer, logon session, integrity level, or other security principal. Account names can be renamed and can require domain lookup; access-control entries store SIDs.", "A SID string contains a revision, identifier authority, and subauthorities. Well-known SIDs identify principals such as Everyone, Local System, and the built-in Administrators group. Local account SIDs normally share a machine-specific domain portion plus a relative identifier."],
    mechanics: ["Group membership expands the identities present in a token. Deny-only groups, restricted SIDs, capabilities, app-container SIDs, and logon SIDs have special authorization roles. A displayed username alone is not a complete security context.", "LookupAccountName resolves a name to SID and domain; LookupAccountSid resolves the other direction. Resolution can fail or contact domain services, so tools should retain the SID string as the stable form."],
    windows: ["whoami /user and whoami /groups show the current token's identities. Process Explorer exposes token groups and attributes. AccessChk accepts users or groups when evaluating access.", "Do not infer privilege merely from an account name containing Administrator. UAC filtering, token type, enabled groups, integrity, and privileges determine the effective context."],
    keys: ["SIDs are authorization identities", "Names are translations", "Tokens contain user and group SIDs", "Group attributes affect use"],
    apis: ["win32security.LookupAccountName", "win32security.LookupAccountSid", "win32security.ConvertSidToStringSid", "win32security.GetTokenInformation"],
    practice: { title: "Resolve the current identity", time: "20 min", intro: "Compare friendly names, raw SIDs, groups, and token attributes.", steps: ["Open the current process token and request TokenUser and TokenGroups.", "Convert every SID to string form and attempt name lookup.", "Mark enabled, deny-only, integrity, and logon-related groups.", "Compare the output with whoami /all.", "Explain why the SID is the stable identity used in an ACE."] },
    check: ["Why do ACLs store SIDs instead of account names?", ["SIDs are stable identifiers used by authorization", "Names cannot contain letters", "SIDs are always shorter", "Names run in kernel mode"], 0, "Names can be changed or ambiguous across authorities, while the SID is the security principal identifier."],
    sources: [["Security identifiers", "https://learn.microsoft.com/windows-server/identity/ad-ds/manage/understand-security-identifiers"]]
  },
  {
    id: "access-tokens", module: "security", title: "Access tokens and security contexts",
    lead: "Read a token as the kernel's packaged security context for a process or impersonating thread.",
    core: ["After authentication, Windows creates access tokens containing the user SID, group SIDs and attributes, privileges, integrity level, token type, session, elevation state, and other restrictions. A process normally has a primary token.", "A thread can use an impersonation token. During an access check, the effective thread token takes precedence; otherwise the process token supplies the subject context."],
    mechanics: ["OpenProcessToken requires a process handle and token access such as TOKEN_QUERY. GetTokenInformation uses information classes such as TokenUser, TokenGroups, TokenPrivileges, TokenElevation, TokenIntegrityLevel, and TokenType.", "Tokens are kernel objects with handles and access control. Duplicating a token does not copy identity into Python data; it creates another token object or handle with a selected primary or impersonation type and impersonation level."],
    windows: ["Process Explorer's Security tab is the quickest visual token view. Compare an ordinary process, elevated process, service, and browser sandbox process to see filtering and integrity in context.", "Close every opened token and process handle. Request TOKEN_QUERY for inspection rather than adjustment or duplication rights you do not need."],
    keys: ["Tokens package effective security state", "Processes use primary tokens", "Threads can impersonate", "Token access rights are separate from contained privileges"],
    apis: ["win32security.OpenProcessToken", "win32security.GetTokenInformation", "win32security.DuplicateTokenEx", "win32api.CloseHandle"],
    practice: { title: "Build a token summary", time: "25 min", intro: "Produce a readable report that explains effective context rather than dumping raw tuples.", steps: ["Open your process token with TOKEN_QUERY.", "Report user SID and name, group count, enabled privileges, elevation type, and integrity level.", "Run once ordinarily and once elevated.", "Compare the reports with Process Explorer.", "Close the token in finally and document access-denied behavior for protected targets."] },
    check: ["Which token does a non-impersonating thread normally use?", ["Its process primary token", "A random service token", "The object's DACL", "The parent PID"], 0, "Without a thread impersonation token, access checks use the process primary token."],
    sources: [["Access tokens", "https://learn.microsoft.com/windows/win32/secauthz/access-tokens"]]
  },
  {
    id: "security-descriptors-dacls-aces", module: "security", title: "Security descriptors, DACLs, and ACEs",
    lead: "Read an object's owner, group, discretionary policy, audit policy, and mandatory labels without losing the order and semantics of ACEs.",
    core: ["A security descriptor can contain owner and primary-group SIDs, a DACL, and a SACL. The DACL governs discretionary access. The SACL contains audit ACEs and mandatory labels and usually requires additional privilege to read or change.", "A DACL is an ordered list of ACEs. Each ACE identifies a SID, allow or deny type, access mask, inheritance flags, and sometimes object-specific details. Canonical ordering matters because access checks process relevant entries in order."],
    mechanics: ["A null DACL grants full access, while an empty DACL grants no access. Those are very different from an absent request to retrieve DACL information. Inheritance can copy or propagate ACEs from a parent and can be protected at the child.", "Generic rights such as GENERIC_READ are mapped to object-specific masks. File read, process query, service start, and event modification use different bits, so an access mask must be interpreted for its object type."],
    windows: ["GetNamedSecurityInfo returns descriptor components for named objects. win32security objects expose DACL and ACE data. AccessChk provides an independent effective-access view.", "Do not rewrite an entire production DACL to change one permission. Preserve unknown ACEs, ordering, inheritance intent, and the original descriptor for restoration."],
    keys: ["DACLs control discretionary access", "ACE order and object type matter", "Null DACL means allow all", "Empty DACL means allow none"],
    apis: ["win32security.GetNamedSecurityInfo", "win32security.SetNamedSecurityInfo", "GetSecurityDescriptorDacl", "GetAce"],
    practice: { title: "Decode a lab file DACL", time: "25 min", intro: "Use a disposable file in your own directory.", steps: ["Create the file and retrieve owner, DACL, and control flags.", "For each ACE, print type, SID string and name, mask, and inheritance flags.", "Translate the mask using file-specific rights.", "Compare the result with icacls and AccessChk.", "Explain whether each ACE is explicit or inherited without modifying it."] },
    check: ["What does an empty DACL grant?", ["Full access", "No access", "Read only", "Administrator access only"], 1, "An empty DACL contains no allowing ACEs. A null DACL is the dangerous grant-all case."],
    sources: [["Security descriptors", "https://learn.microsoft.com/windows/win32/secauthz/security-descriptors"]]
  },
  {
    id: "access-check", module: "security", title: "How Windows performs an access check",
    lead: "Walk requested rights through token identities, deny and allow ACEs, privileges, mandatory policy, and the final granted-access mask.",
    core: ["The caller requests an access mask while opening an object. Windows maps generic rights, considers the owner and applicable privileges, then evaluates relevant DACL ACEs against enabled token SIDs until all requested bits are granted or a required bit is denied or remains ungranted.", "A deny ACE affects still-requested rights when its SID applies. An allow ACE grants matching remaining bits. Canonical ACL construction places explicit denies before explicit allows, followed by inherited entries in canonical groups."],
    mechanics: ["MAXIMUM_ALLOWED asks Windows to compute all access the token can obtain, but specific least-privilege rights make intent clearer. A handle stores the granted mask, so changing a DACL later does not usually revoke rights already present in that open handle.", "Mandatory integrity control adds no-write-up style restrictions after discretionary logic. Protected-process rules, share modes, object state, and application policy can also cause failure even when a simplified DACL calculation appears permissive."],
    windows: ["AccessCheck can evaluate a descriptor with an impersonation token and generic mapping. AccessChk gives a practical view but results should be tied to exact object type, principal, and requested operation.", "When diagnosing access denied, log the function, object, requested mask, error number, token elevation and integrity, and target descriptor. Do not immediately retry with all access."],
    keys: ["Access checks evaluate requested bits", "Only applicable token SIDs participate", "Existing handles retain granted rights", "DACLs are not the only failure source"],
    apis: ["win32security.AccessCheck", "win32security.MapGenericMask", "GetEffectiveRightsFromAclW", "GetLastError"],
    practice: { title: "Predict then verify access", time: "30 min", intro: "Use a disposable file and a read-only test token.", steps: ["Retrieve the file security descriptor and your token groups.", "Predict whether FILE_GENERIC_READ and FILE_GENERIC_WRITE should be granted.", "Run AccessCheck with a file generic mapping.", "Verify by opening the file with the corresponding specific rights.", "Change only your lab file's ACL, repeat, then restore the original descriptor."] },
    check: ["Does changing a DACL normally remove rights from an already open handle?", ["Yes, instantly", "No, the handle retains its granted-access mask", "Only if the PID is odd", "Only for Registry keys"], 1, "The access decision was captured when the handle was created. Revocation requires stronger object-specific mechanisms or closing existing handles."],
    sources: [["How DACLs control access", "https://learn.microsoft.com/windows/win32/secauthz/how-dacls-control-access-to-an-object"]]
  },
  {
    id: "privileges-impersonation", module: "security", title: "Privileges and impersonation",
    lead: "Distinguish object rights from token privileges, then use impersonation as a scoped change of subject identity.",
    core: ["A privilege authorizes a system-wide operation such as debugging processes, backing up files, restoring data, or shutting down the computer. The privilege must be present in the token and often explicitly enabled. Enabling cannot grant a privilege the account was never assigned.", "Impersonation lets a thread temporarily use a client's token so access checks reflect the client. Servers use this to access files or Registry data on a caller's behalf without permanently changing the whole process identity."],
    mechanics: ["LookupPrivilegeValue resolves a privilege LUID. AdjustTokenPrivileges changes attributes and can return success while reporting ERROR_NOT_ALL_ASSIGNED through last error. Correct code checks that condition.", "DuplicateTokenEx selects token type, rights, and impersonation level. Primary tokens can be used for process creation under strict privilege and access requirements; impersonation tokens are used on threads. Always revert impersonation in finally."],
    windows: ["The get_my_leverage practice combines process access, token query and duplication, privileges, CreateProcessAsUser, and extensive handle cleanup. Run only against an account and process you own in a controlled VM.", "Security software and enterprise policy may block token-based process creation even for administrators. Treat that as a boundary to explain, not bypass."],
    keys: ["Privileges differ from object access rights", "A privilege must already exist in the token", "Impersonation changes the thread subject", "Token duplication and process creation require precise rights"],
    apis: ["win32security.LookupPrivilegeValue", "win32security.AdjustTokenPrivileges", "win32security.DuplicateTokenEx", "win32process.CreateProcessAsUser", "win32security.RevertToSelf"],
    practice: { title: "Audit the leverage chain", time: "40 min", intro: "Analyze get_my_leverage.py as a security contract before executing it in a VM.", steps: ["List every required process, token, and privilege access right.", "Mark which handles each function acquires and who closes them.", "Add an ERROR_NOT_ALL_ASSIGNED check after AdjustTokenPrivileges.", "Run against a process you own and compare source and child tokens.", "Record expected access-denied branches and explain the policy rather than increasing rights blindly."] },
    check: ["Can AdjustTokenPrivileges add a privilege absent from the token?", ["Yes", "No", "Only for files", "Only in 32-bit processes"], 1, "It can enable, disable, or remove privileges already represented in the token, not grant a new account right."],
    sources: [["Privileges", "https://learn.microsoft.com/windows/win32/secauthz/privileges"], ["Impersonation", "https://learn.microsoft.com/windows/win32/secauthz/impersonation"]]
  },
  {
    id: "uac-integrity", module: "security", title: "UAC and integrity levels",
    lead: "Separate administrator membership, elevation, integrity level, virtualization, and processor mode.",
    core: ["With UAC, an administrator normally launches desktop applications using a filtered token. An elevation request can start a new process with the linked full token after consent or credentials. Elevation is not an in-place transformation of the existing process.", "Mandatory integrity control assigns integrity labels to tokens and objects. The common policy prevents lower-integrity subjects from writing to higher-integrity objects even if discretionary permissions look permissive. Browsers and sandboxed content may run at Low integrity; ordinary desktop apps typically run at Medium; elevated apps run at High."],
    mechanics: ["An executable manifest can request asInvoker, highestAvailable, or requireAdministrator behavior. The shield and consent UI express an elevation boundary, not a guarantee that the program is safe.", "UAC file and Registry virtualization helps selected legacy 32-bit unelevated applications by redirecting writes. It is not available to every application and should not be used as a design strategy."],
    windows: ["Process Explorer shows integrity, elevation, virtualization, and token groups. Compare the same executable started from ordinary and elevated terminals. Both remain user mode.", "Integrity is mandatory policy layered with DACLs. A high-integrity token still needs the requested object rights, and protected system processes can have additional restrictions."],
    keys: ["Administrator membership is not the same as elevation", "Elevation starts a process with a different token", "Integrity restricts cross-level writes", "User mode and integrity are separate axes"],
    apis: ["GetTokenInformation TokenElevationType", "GetTokenInformation TokenIntegrityLevel", "ShellExecute runas", "CheckTokenMembership"],
    practice: { title: "Compare filtered and elevated tokens", time: "20 min", intro: "Use the same harmless Python token-summary script in both contexts.", steps: ["Run the summary normally and record elevation type, integrity, groups, and enabled privileges.", "Run it from an elevated terminal and record the same fields.", "Compare both processes in Process Explorer.", "Attempt a safe read-only query and one reversible protected write in a VM, recording the exact result.", "Explain why neither process is kernel mode."] },
    check: ["What does UAC elevation change?", ["The executable's CPU architecture", "The process security token and integrity context", "User-mode code into a driver", "The page size"], 1, "An elevated process receives a different security context while remaining a user-mode process."],
    sources: [["How UAC works", "https://learn.microsoft.com/windows/security/application-security/application-control/user-account-control/how-it-works"], ["Mandatory integrity control", "https://learn.microsoft.com/windows/win32/secauthz/mandatory-integrity-control"]]
  },
  {
    id: "concurrency-problems", module: "synchronisation", title: "Why concurrent code goes wrong",
    lead: "Concurrency creates valid interleavings the programmer did not anticipate, especially around shared mutable state and resource lifetimes.",
    core: ["Threads in one process share memory, handles, and other process state. Two individually correct operations can combine incorrectly when the scheduler interleaves them. Timing-dependent failures are hard to reproduce because logging, debugging, and machine load change timing.", "The first design question is what state is shared, who owns it, and which invariants span more than one operation. Avoiding shared mutation, partitioning ownership, and passing immutable messages can remove the need for many locks."],
    mechanics: ["A check-then-act sequence is unsafe when another thread can change the condition between the check and action. Lifetime races occur when one thread closes or frees a resource while another still expects to use it.", "Thread safety is a contract over a whole operation. Python syntax, the GIL, or a library's internal lock does not automatically protect an application-level invariant that spans several calls."],
    windows: ["Windows supplies dispatcher objects that let threads wait efficiently rather than spin. User-mode locks can avoid kernel transitions when uncontended, while kernel objects support cross-process naming and waits.", "Process Explorer exposes thread counts and stacks, but a race may need logging, Application Verifier, a debugger, or a deliberately widened timing window to reproduce."],
    keys: ["Interleavings create bugs", "Protect invariants, not lines", "Ownership can remove sharing", "Waiting should be efficient"],
    apis: ["threading.Lock", "win32event.WaitForSingleObject", "WaitForMultipleObjects"],
    practice: { title: "Inventory shared state", time: "20 min", intro: "Analyze party.py before adding or removing any lock.", steps: ["List every global, handle, object, and log sink used by multiple threads.", "Mark each read-modify-write sequence and resource lifetime.", "State the invariant for cake pieces in one sentence.", "Identify which event coordinates phase and which mutex protects state.", "Explain one redesign that uses a queue or single owner instead of shared mutation."] },
    check: ["What is the best first question in a concurrency review?", ["How many colors are in the UI?", "What mutable state and lifetime are shared?", "Can every thread have high priority?", "Can waits be removed?"], 1, "Concurrency correctness starts with shared state, ownership, invariants, and lifetime."],
    sources: [["Synchronization", "https://learn.microsoft.com/windows/win32/sync/synchronization"]]
  },
  {
    id: "atomicity-races", module: "synchronisation", title: "Atomicity and race conditions",
    lead: "One source-code line can become several interruptible machine operations, so reason about indivisible effects rather than visual brevity.",
    core: ["A race condition exists when correctness depends on the relative timing of concurrent operations. Increment commonly means load, add, and store. Two threads can load the same old value and both store the same incremented result, losing one update.", "Atomic means no observer can see an intermediate state for the defined operation. CPU instructions have architecture-specific atomicity rules, while operating systems and languages expose stronger atomic or locked operations with ordering guarantees."],
    mechanics: ["The CPython GIL is not an application transaction. Bytecode boundaries can change, extensions can release the GIL, and a multi-step invariant remains vulnerable. Use a lock, queue, immutable handoff, database transaction, or documented atomic primitive.", "Reproduction improves by splitting the read and write, adding a barrier or sleep between them, using many iterations, and checking a deterministic invariant. The widened race demonstrates the model; it does not imply production timing."],
    windows: ["InterlockedIncrement and related Interlocked functions provide atomic operations with documented memory-order effects for aligned values. They are suitable for simple counters and flags, not an arbitrary multi-field invariant.", "The synchronization lecture's counter example should be run both without protection and with a lock, repeating enough times to observe failure."],
    keys: ["Source lines are not atomic units", "Races violate invariants under some interleavings", "The GIL is not a general transaction", "Interlocked operations solve narrow cases"],
    apis: ["InterlockedIncrement", "InterlockedCompareExchange", "threading.Lock"],
    practice: { title: "Make a lost update visible", time: "20 min", intro: "Use a barrier to control the unsafe interleaving.", steps: ["Have two threads read a shared counter into local variables.", "Use a barrier so both reads finish before either write.", "Let both write local plus one and assert the expected final value.", "Protect the whole read-modify-write with a lock and repeat.", "Explain why changing it to counter += 1 is not a portable correctness proof."] },
    check: ["Why can counter += 1 race?", ["It is always a single locked CPU instruction", "It can comprise several read, modify, and write steps", "Integers are file handles", "The scheduler disables increments"], 1, "The application-level update can be interrupted or interleaved unless a documented synchronization guarantee makes it atomic."],
    sources: [["Interlocked variable access", "https://learn.microsoft.com/windows/win32/sync/interlocked-variable-access"]]
  },
  {
    id: "critical-sections-mutexes", module: "synchronisation", title: "Critical sections and mutexes",
    lead: "Use a process-local lock for cheap thread coordination and a mutex when kernel visibility, waiting, ownership, or cross-process naming is needed.",
    core: ["A critical section protects threads within one process and normally stays in user mode when uncontended. A mutex is a waitable kernel object, can be named and opened by another process, and tracks an owning thread.", "Lock the smallest complete invariant, not necessarily the fewest lines. Holding a lock during slow I/O or arbitrary callbacks increases contention and deadlock risk, but releasing it between a check and dependent action breaks correctness."],
    mechanics: ["A mutex is recursive for its owning thread, with a matching release count. If the owner terminates without release, a waiter receives WAIT_ABANDONED. It owns the mutex but must assume protected data may be inconsistent.", "Use try/finally immediately after successful acquisition. Do not release after a timeout or failed wait. Document lock order when more than one lock may be acquired."],
    windows: ["win32event.CreateMutex returns a handle. WaitForSingleObject can return WAIT_OBJECT_0, WAIT_ABANDONED, WAIT_TIMEOUT, or WAIT_FAILED. ReleaseMutex fails if the caller is not the owner.", "party.py protects the check, delay, decrement, and logically related result as one critical section. Its abandoned branch should warn and validate shared state before continuing."],
    keys: ["Critical sections are process-local", "Mutexes are kernel waitable objects with ownership", "WAIT_ABANDONED grants ownership with a warning", "Release only after acquisition"],
    apis: ["win32event.CreateMutex", "win32event.WaitForSingleObject", "win32event.ReleaseMutex", "EnterCriticalSection", "LeaveCriticalSection"],
    practice: { title: "Protect the cake invariant", time: "35 min", intro: "Complete the mutex path in party.py and test both success and failure branches.", steps: ["Create the mutex without initial ownership.", "Interpret WAIT_OBJECT_0, WAIT_ABANDONED, WAIT_TIMEOUT, and unexpected results explicitly.", "Protect the entire remaining-cake check and decrement.", "Release in finally only after a successful or abandoned acquisition.", "Terminate a disposable owner during a lab-only run and explain why data validation is required after abandonment."] },
    check: ["What should a waiter do after WAIT_ABANDONED?", ["Assume it did not acquire the mutex", "Treat it as ownership but validate possibly inconsistent state", "Release every mutex in the system", "Convert it to WAIT_TIMEOUT"], 1, "Windows gives the waiter ownership, but the previous owner ended without normal cleanup."],
    sources: [["Mutex objects", "https://learn.microsoft.com/windows/win32/sync/mutex-objects"]]
  },
  {
    id: "semaphores", module: "synchronisation", title: "Semaphores",
    lead: "Use a counted gate when up to N concurrent users may consume a limited resource.",
    core: ["A semaphore has a current count and maximum count. A successful wait decrements the count. ReleaseSemaphore increments it and can release one or more waiters. When the count is zero, waits block or time out.", "A semaphore does not track ownership. Any thread with modify rights can release it, so over-release is a logic bug. A max-count-one semaphore resembles a binary gate but does not gain mutex ownership or abandonment semantics."],
    mechanics: ["Semaphores model bounded resources such as database connections, worker slots, or in-flight operations. They do not protect an arbitrary data structure unless the count and access protocol are designed for that purpose.", "Always pair each successful acquisition with exactly one release in finally. Cancellation and exceptions must not leak a permit."],
    windows: ["CreateSemaphore sets initial and maximum counts and an optional name. ReleaseSemaphore can report the previous count. OpenSemaphore connects another process to a named semaphore when access allows.", "Process Explorer and WinObj can show the handle and named object, while application logging should show permit acquisition and release because the count is the critical state."],
    keys: ["Semaphore counts available permits", "There is no owner", "Max count is enforced", "Each acquired permit needs one release"],
    apis: ["win32event.CreateSemaphore", "win32event.OpenSemaphore", "win32event.ReleaseSemaphore", "win32event.WaitForSingleObject"],
    practice: { title: "Limit concurrent work", time: "20 min", intro: "Allow only three of ten workers into a simulated resource.", steps: ["Create a semaphore with initial and maximum count three.", "Start ten workers that wait, log entry, pause, log exit, and release.", "Track an independent active count under a small lock and assert it never exceeds three.", "Add a timeout path that does not release a permit it never acquired.", "Inspect the semaphore handle and, if named, the object in WinObj."] },
    check: ["How does a semaphore differ from a mutex?", ["A semaphore has counted permits and no owner", "A semaphore cannot be waited on", "A mutex has unlimited permits", "A mutex is only for files"], 0, "Semaphores count capacity, while mutexes represent exclusive ownership and support abandonment reporting."],
    sources: [["Semaphore objects", "https://learn.microsoft.com/windows/win32/sync/semaphore-objects"]]
  },
  {
    id: "events-waits", module: "synchronisation", title: "Events and waitable objects",
    lead: "Represent a condition with an event and interpret wait results as explicit control-flow outcomes.",
    core: ["An event has signaled or nonsignaled state. A manual-reset event stays signaled until ResetEvent and can release all current and later waiters. An auto-reset event normally releases one waiter and automatically returns to nonsignaled.", "Wait functions apply to events and many other waitable handles, including mutexes, semaphores, processes, threads, and timers. The object's signaled meaning differs by type."],
    mechanics: ["WAIT_OBJECT_0 means the single object satisfied the wait. WAIT_TIMEOUT means the deadline expired and nothing was acquired. WAIT_ABANDONED applies to mutex ownership. WAIT_FAILED requires immediate error retrieval. Multiple-object waits offset results by the object's array index.", "A wait closes no handle and does not automatically reset a manual-reset event. Lifetime and reset policy remain the application's responsibility."],
    windows: ["inviter.py creates a named manual-reset event and signals every invitee. invitee.py opens it with SYNCHRONIZE access and treats only WAIT_OBJECT_0 as success. Both close their handles in finally.", "party.py uses the event as a phase gate and a mutex for exclusive cake access. This separation illustrates that signaling and mutual exclusion solve different problems."],
    keys: ["Manual reset releases a group until reset", "Auto reset releases one waiter", "Wait results require branches", "Waiting and handle cleanup are separate"],
    apis: ["win32event.CreateEvent", "win32event.OpenEvent", "win32event.SetEvent", "win32event.ResetEvent", "win32event.WaitForSingleObject"],
    practice: { title: "Run inviter and invitees", time: "30 min", intro: "Complete the repository pair and explain every state transition.", steps: ["Start inviter.py and verify the event begins nonsignaled.", "Start several invitee.py processes and confirm they wait without consuming CPU.", "Signal once and verify a manual-reset event releases all waiters.", "Change only the design to auto reset, predict and observe the difference.", "Handle missing event, timeout, success, and cleanup paths explicitly."] },
    check: ["What does WAIT_OBJECT_0 mean for a single event wait?", ["The timeout elapsed", "The event satisfied the wait", "The handle was closed", "The mutex was abandoned"], 1, "The event was in or entered the signaled state according to its reset behavior."],
    sources: [["Event objects", "https://learn.microsoft.com/windows/win32/sync/event-objects"], ["WaitForSingleObject", "https://learn.microsoft.com/windows/win32/api/synchapi/nf-synchapi-waitforsingleobject"]]
  },
  {
    id: "deadlocks-starvation", module: "synchronisation", title: "Deadlocks, starvation, and safe design",
    lead: "Prevent liveness failures with ownership, ordering, bounded waits, cancellation, and observability.",
    core: ["Deadlock requires a cycle of dependencies in which each participant waits for a resource held by another. The classic conditions are mutual exclusion, hold-and-wait, no forced preemption, and circular wait. Breaking any one prevents that specific deadlock.", "Starvation means a participant can wait indefinitely while others progress. Livelock means participants keep changing state but make no useful progress. Correctness includes liveness, not only protected data."],
    mechanics: ["Use one global lock order, avoid calls into unknown code while holding locks, minimize nested acquisition, and move blocking I/O outside critical sections when the invariant permits. Bounded waits create a diagnostic or recovery branch but do not by themselves repair shared state.", "Cancellation should wake waiters and let owners unwind. Logging must include thread identity, requested resource, acquisition time, hold duration, and release so a wait-for graph can be reconstructed."],
    windows: ["Wait Chain Traversal can diagnose some blocked thread chains, and Process Explorer can expose stacks. A dump captured during the hang is often more useful than logs after forced termination.", "Abandoned mutexes report a failed owner but not whether the data is valid. Design a validation or recovery strategy, or prefer a model in which process death cannot leave shared mutable data inconsistent."],
    keys: ["Deadlock is a dependency cycle", "Starvation is indefinite unfair delay", "A lock order prevents many cycles", "Timeouts need a defined recovery path"],
    apis: ["WaitForMultipleObjects", "RegisterWaitForSingleObject", "OpenThreadWaitChainSession"],
    practice: { title: "Diagnose a deliberate deadlock", time: "25 min", intro: "Use only a disposable two-lock Python program.", steps: ["Create two workers that acquire locks in opposite order with a barrier between acquisitions.", "Reproduce the hang and capture thread stacks.", "Draw the wait-for cycle.", "Fix both workers to acquire locks in one documented order.", "Add bounded diagnostics without pretending a timeout makes partially updated data safe."] },
    check: ["Which design most directly prevents a two-lock circular wait?", ["Give both threads higher priority", "Use one consistent lock acquisition order", "Add more worker threads", "Rename the locks"], 1, "A global acquisition order removes the cycle that circular wait requires."],
    sources: [["Using critical sections safely", "https://learn.microsoft.com/windows/win32/sync/using-critical-section-objects"]]
  },

  {
    id: "why-ipc", module: "ipc", title: "Why processes need IPC",
    lead: "Process isolation prevents accidental sharing, so communication needs an explicit channel, protocol, synchronization plan, and security boundary.",
    core: ["Processes have separate virtual address spaces and handle tables. A pointer or ordinary Python object in one process is meaningless in another. IPC deliberately transports bytes, shares backing, duplicates handles, or routes requests through an OS-managed endpoint.", "Choosing IPC requires more than speed. Consider relationship, direction, message boundaries, local or network scope, throughput, latency, persistence, discovery, trust, identity, cancellation, and failure recovery."],
    mechanics: ["A protocol defines framing, encoding, versioning, size limits, timeouts, errors, and shutdown. Streams do not preserve application message boundaries unless the mechanism or protocol adds them.", "Synchronization objects can coordinate processes but do not carry arbitrary payload data. Shared memory carries data but requires separate coordination. Pipes and sockets often combine transport blocking with data transfer."],
    windows: ["Named kernel objects can be found by name when security allows; handles can also be inherited or duplicated. Local and Global namespaces affect visibility across sessions.", "Process Explorer and Handle reveal endpoints and handles. Process Monitor shows pipe and file operations. Network tools are needed for socket endpoints."],
    keys: ["Isolation makes communication explicit", "IPC needs a protocol", "Transport and synchronization are distinct", "Endpoint security is part of design"],
    apis: ["DuplicateHandle", "CreatePipe", "CreateNamedPipeW", "CreateFileMappingW", "socket"],
    practice: { title: "Write an IPC decision record", time: "20 min", intro: "Choose mechanisms for three scenarios and defend the tradeoffs.", steps: ["Compare parent-to-child command output, unrelated local client-server requests, and high-volume shared records.", "For each, state topology, framing, identity, timeout, cleanup, and failure behavior.", "Select anonymous pipe, named pipe, shared memory, socket, or file and justify it.", "Name one security risk and one evidence tool for each choice.", "Explain why fastest is not automatically best."] },
    check: ["Why can one process not send another a normal pointer?", ["Pointers are always text", "The address is interpreted in a different virtual address space", "Windows blocks all numbers", "PIDs use the same value"], 1, "The receiving process's mapping at that virtual address is unrelated unless explicit shared memory was established."],
    sources: [["Interprocess communications", "https://learn.microsoft.com/windows/win32/ipc/interprocess-communications"]]
  },
  {
    id: "anonymous-pipes", module: "ipc", title: "Anonymous pipes",
    lead: "Build a one-way byte stream between related processes by transferring carefully selected inheritable handles.",
    core: ["CreatePipe returns read and write handles for a byte stream. Anonymous pipes are convenient for parent-child standard-input, standard-output, and standard-error redirection. They are implemented using the named-pipe mechanism but normally discovered through inherited handles.", "Inheritance has two gates: the handle must carry HANDLE_FLAG_INHERIT and process creation must enable inheritance or provide an explicit allowed-handle list. The parent must disable inheritance on its private endpoint."],
    mechanics: ["End-of-stream occurs only after every write handle referring to the pipe is closed. An accidentally inherited or retained write handle makes the reader block forever. Close unused endpoints in parent and child immediately after creation.", "ReadFile returns bytes. Console programs may emit an OEM, preferred locale, UTF-8, or application-selected encoding. Decode only at the boundary with an explicit policy and replacement or reporting strategy."],
    windows: ["pipe_one.py constructs cmd1 | cmd2. STARTUPINFO with STARTF_USESTDHANDLES gives child one the parent's stdin and pipe write output, then gives child two the pipe read input and an output pipe back to the parent.", "The parent reads until ERROR_BROKEN_PIPE, waits on both process handles, and closes every pipe, process, and thread handle. Cleanup order determines whether EOF can arrive."],
    keys: ["Anonymous pipes are byte streams", "Inheritance must be explicit", "All writers must close for EOF", "STARTUPINFO redirects standard handles"],
    apis: ["win32pipe.CreatePipe", "win32api.SetHandleInformation", "win32process.STARTUPINFO", "win32process.CreateProcess", "win32file.ReadFile"],
    practice: { title: "Complete a two-command pipeline", time: "45 min", intro: "Use pipe_one.py and make every endpoint owner visible on paper first.", steps: ["Create inheritable pipe handles and clear inheritance on parent-only endpoints.", "Build STARTUPINFO for both children and launch with handle inheritance enabled.", "Close each unused parent endpoint immediately after the relevant child starts.", "Read output until ERROR_BROKEN_PIPE, then wait for both process handles.", "Decode explicitly and close child process and thread handles in finally."] },
    check: ["Why can a pipe reader wait forever after children exit?", ["A write handle is still open somewhere", "The PID was converted to hex", "The pipe contains text", "ReadFile closes handles automatically"], 0, "EOF is delivered only when no write-side handle remains open."],
    sources: [["Anonymous pipe operations", "https://learn.microsoft.com/windows/win32/ipc/anonymous-pipe-operations"]]
  },
  {
    id: "named-pipes", module: "ipc", title: "Named-pipe servers and clients",
    lead: "Create a discoverable local or remote-capable endpoint with defined instances, mode, framing, identity, and disconnect behavior.",
    core: ["A named pipe uses a path such as \\\\.\\pipe\\ILOVEOS. The server creates one or more instances and waits for clients. A client waits for availability and opens an instance through CreateFile.", "Byte mode presents a stream; message mode preserves write boundaries subject to buffer behavior and can report ERROR_MORE_DATA when a read buffer is too small. The read mode must match the protocol's expectations."],
    mechanics: ["A server lifecycle is create, connect, exchange, flush if required, disconnect, and close. ConnectNamedPipe can report ERROR_PIPE_CONNECTED when the client connected between creation and the call; this is a successful race outcome to handle.", "Named pipes are securable. A server can authenticate or impersonate a connected client, but must validate messages, limit sizes, use timeouts or cancellation, and revert impersonation."],
    windows: ["whats_my_name.py is the client half: WaitNamedPipe, CreateFile, SetNamedPipeHandleState, encoded write, response read, decode, and cleanup. Its PIPE_NAME, encoding, and framing must exactly match the server.", "Process Explorer or Handle can reveal pipe handles; Process Monitor can capture CreateFile and pipe operations. A name proves discovery, not trust."],
    keys: ["Servers create instances and clients open them", "Byte and message modes differ", "Connection races have documented outcomes", "Pipe ACLs and client identity matter"],
    apis: ["win32pipe.CreateNamedPipe", "win32pipe.ConnectNamedPipe", "win32pipe.WaitNamedPipe", "win32pipe.SetNamedPipeHandleState", "win32file.CreateFile"],
    practice: { title: "Finish the named-pipe identity client", time: "35 min", intro: "Pair whats_my_name.py with a known lab server.", steps: ["Validate the exact \\\\.\\pipe\\ name, encoding, and message format.", "Wait with a bounded timeout, open the pipe, and set the intended read mode.", "Encode the local username with an explicit delimiter or length rule.", "Handle ERROR_MORE_DATA or looped reads according to the protocol.", "Close the handle in finally and inspect the endpoint with Handle while connected."] },
    check: ["What does a named-pipe name provide by itself?", ["Discovery of the endpoint", "Authentication of every client", "Encryption of all messages", "Automatic JSON framing"], 0, "The name lets a client locate the pipe. Security descriptors and protocol behavior provide authorization and trust controls."],
    sources: [["Named pipes", "https://learn.microsoft.com/windows/win32/ipc/named-pipes"]]
  },
  {
    id: "file-mappings-shared-memory", module: "ipc", title: "File mappings and shared memory",
    lead: "Share high-throughput memory while supplying the synchronization and protocol that raw pages do not provide.",
    core: ["CreateFileMapping creates a section backed by a file or paging file. MapViewOfFile maps some or all of it into the caller. Another process opens the mapping by name or receives a handle, then maps its own view.", "The virtual addresses can differ, so shared data must use offsets or self-contained values rather than raw process pointers. Fixed-width structures need byte order, alignment, version, and size rules."],
    mechanics: ["A safe protocol may use a header with magic, version, capacity, payload length, sequence, and status. An event can signal data readiness; a mutex can serialize writers. A sequence counter can help a reader detect a torn update.", "Unmap every view and close every section, file, event, and mutex handle. Flushing a view addresses persistence requirements for file-backed mappings but does not replace application-level synchronization."],
    windows: ["VMMap shows mapped-file and shared regions. Process Explorer and WinObj show Section and synchronization handles. Handle can reveal the underlying file when a file-backed mapping exists.", "Shared memory is powerful but expands the trusted computing boundary because malformed lengths or inconsistent versions can make another process interpret bytes unsafely."],
    keys: ["Mappings share backing, not virtual addresses", "Use offsets instead of pointers", "Define layout and synchronization", "Views and handles have separate cleanup"],
    apis: ["CreateFileMappingW", "OpenFileMappingW", "MapViewOfFile", "FlushViewOfFile", "UnmapViewOfFile"],
    practice: { title: "Design a versioned shared record", time: "35 min", intro: "Implement one producer and one consumer with a named event.", steps: ["Define a fixed header and maximum payload size.", "Create and zero the mapping before publishing readiness.", "Write payload first, then length and sequence under the chosen synchronization rule.", "Have the consumer validate magic, version, bounds, and stable sequence before decoding.", "Signal shutdown, unmap both views, and close all handles."] },
    check: ["Why should shared structures avoid raw pointers?", ["Pointers are too small", "Each process can map the view at a different virtual address", "Pointers cannot represent numbers", "MapViewOfFile converts them to files"], 1, "An address meaningful in one process is not guaranteed to identify the same location in another."],
    sources: [["Creating named shared memory", "https://learn.microsoft.com/windows/win32/memory/creating-named-shared-memory"]]
  },
  {
    id: "choose-ipc", module: "ipc", title: "Choosing an IPC mechanism",
    lead: "Select IPC by topology, semantics, security, operability, and failure behavior rather than by habit.",
    core: ["Anonymous pipes fit related process streams and standard-handle redirection. Named pipes fit local client-server request channels and can support remote access. Shared memory fits high-volume local data when you can design synchronization. Sockets fit network and cross-platform communication. Files fit durable, decoupled exchange.", "RPC and higher-level frameworks add interface definitions, marshaling, discovery, authentication, and versioning. Their overhead often buys clearer contracts and safer evolution."],
    mechanics: ["Backpressure determines what happens when the producer is faster. Bounded buffers block, reject, or drop according to policy. Timeouts and cancellation define how components recover when peers hang or exit.", "Security questions include who can create or open the endpoint, how peer identity is established, whether content needs confidentiality or integrity, and how untrusted sizes and commands are validated."],
    windows: ["pipe_two.py generalizes the anonymous pipeline to N commands. The hard part is an ownership table: for each stage, define which read and write endpoints the parent and child keep, inherit, and close.", "Operational evidence should include endpoint name, owning PID, connection timing, transmitted byte counts, exit codes, and failures without logging sensitive payloads."],
    keys: ["Match mechanism to topology and semantics", "Backpressure is a protocol decision", "Security begins at endpoint creation", "Ownership tables prevent pipe leaks"],
    apis: ["CreatePipe", "CreateNamedPipeW", "CreateFileMappingW", "WSASocketW"],
    practice: { title: "Generalize to an N-stage pipeline", time: "50 min", intro: "Complete pipe_two.py using a table before writing loop logic.", steps: ["For N commands, draw N minus one inter-child pipes plus one final output pipe.", "For each stage, assign stdin, stdout, stderr, and every inherited handle.", "Launch the stage, then close parent copies that no future child needs.", "Read the final output and wait for every process, preserving each exit code.", "Test one, two, and several stages plus a failing command and verify no read hangs."] },
    check: ["What is the most common cause of an anonymous pipeline that never reaches EOF?", ["Too many command names", "An unintended write handle remains open", "The output is Unicode", "The process has a token"], 1, "A pipe remains writable until every reference to its write side closes, including inherited copies."],
    sources: [["IPC mechanisms", "https://learn.microsoft.com/windows/win32/ipc/interprocess-communications"]]
  },

  {
    id: "hooking-injection", module: "hooking-injection", title: "Injection and hooking explained",
    lead: "Injection places code or a module into another execution context; hooking redirects an existing call, message, or event path.",
    core: ["Injection and hooking are technique families, not vulnerabilities by themselves. Debuggers, profilers, accessibility tools, compatibility layers, security products, plugins, and malware can all alter runtime behavior for different purposes.", "Injection supplies code or data to the target. Hooking changes where execution or an event is routed. Many hook designs require code already present in the target, so an injection or supported plugin mechanism may precede the hook."],
    mechanics: ["Any cross-process technique requires suitable access, matching architecture, correct calling conventions, safe lifetime, and coordination with concurrently executing threads. Security controls may prohibit or monitor the operation.", "The least risky learning path starts with static PE inspection, module inventories, documented in-process hooks on your own window, and detection evidence. Do not test against third-party or protected processes."],
    windows: ["WoW64 redirection is a compatibility example of intercepted behavior, while IAT replacement redirects imported function calls within a process. SetWindowsHookEx provides documented hooks for selected message and input event flows.", "Process Explorer, ListDLLs, Process Monitor, Sigcheck, and Autoruns answer different provenance questions: what is loaded, when it loaded, where it came from, whether it is signed, and how it starts."],
    keys: ["Injection adds code or a module", "Hooking redirects behavior", "The same mechanism can have legitimate or malicious uses", "Authorization and scope are mandatory"],
    apis: ["SetWindowsHookExW", "LoadLibraryW", "GetProcAddress", "EnumProcessModules"],
    practice: { title: "Classify behavior without executing it", time: "20 min", intro: "Analyze four scenarios on paper before touching any hooking API.", steps: ["Classify a debugger breakpoint, accessibility keyboard hook, plugin DLL, and unauthorized credential-stealing hook.", "For each, identify injection, redirection, or both.", "State the required trust boundary and user consent.", "Choose the evidence tools that would reveal modules, event timing, persistence, and signatures.", "Define a safe lab scope limited to binaries and processes you own."] },
    check: ["Which statement is accurate?", ["Every hook is malware", "Hooking redirects an execution or event path", "Injection always changes a file on disk", "Hooks run only in kernel mode"], 1, "Hooking describes behavior redirection. Legitimacy depends on authorization, purpose, and implementation."],
    sources: [["About hooks", "https://learn.microsoft.com/windows/win32/winmsg/about-hooks"]]
  },
  {
    id: "startup-code-loading", module: "hooking-injection", title: "Loading code during process startup",
    lead: "Understand how loader-controlled dependencies, supported plugins, and configuration can cause code to enter a process before its main logic runs.",
    core: ["The Windows loader maps implicit dependencies before the executable entry point. Adding an import to a disposable executable therefore causes the referenced matching-architecture DLL to load during startup and run its minimal initialization path.", "Applications can also provide supported plugin directories, COM extension points, debuggers, compatibility shims, and other documented extensibility. Historical global injection settings are heavily constrained or unsafe and should not be used as a general plugin design."],
    mechanics: ["Startup loading depends on PE architecture, import names, export resolution, DLL search order, loader policy, signatures, and security controls. Modifying an executable invalidates its signature and can make it untrustworthy or unusable.", "DllMain runs under loader constraints and should not perform complex work. A well-designed plugin exposes an explicit initialization function called after the loader finishes."],
    windows: ["The repository's how_to_load exercise used matching 32-bit lab artifacts and rebuilt an import table. Preserve that exercise only as an isolated demonstration on copies you own.", "Process Monitor Image Load events and ListDLLs establish actual module path. Sigcheck establishes signature and hash. Static import inspection establishes declared dependency."],
    keys: ["Implicit imports load before the main entry point", "Architecture must match", "DLL search is security-sensitive", "Binary modification breaks signatures"],
    apis: ["LoadLibraryExW", "SetDefaultDllDirectories", "AddDllDirectory", "GetModuleFileNameW"],
    practice: { title: "Audit a controlled startup load", time: "30 min", intro: "Reuse the loader lab evidence without expanding its scope.", steps: ["Work only with copies of the supplied x86 lab executable and DLL.", "Hash both files and verify matching architecture.", "Record the modified import and capture the resulting Image Load path.", "Compare hashes and signature status before and after modification.", "Explain how a supported plugin interface would avoid patching the host binary."] },
    check: ["What is the safest production extensibility choice?", ["Patch every executable import table", "Use the application's documented plugin mechanism", "Disable DLL search protections", "Run all code as SYSTEM"], 1, "Supported plugin contracts define compatibility, consent, initialization, and removal more safely than binary patching."],
    sources: [["DLL security", "https://learn.microsoft.com/windows/win32/dlls/dynamic-link-library-security"]]
  },
  {
    id: "remote-memory-threads", module: "hooking-injection", title: "Remote memory and remote threads",
    lead: "Study the cross-process resource chain and its detection surface without turning the lesson into an operational intrusion recipe.",
    core: ["Windows exposes APIs that can open another process, allocate in its address space, copy bytes, and create or direct execution there when the caller has required access. Debuggers and diagnostics use cross-process memory operations, and defenders monitor the same chain because it is commonly abused.", "Every step is object- and rights-based: process discovery, OpenProcess with specific access, a remote allocation, a write, an execution mechanism, completion, remote release, and local handle cleanup."],
    mechanics: ["Architecture, address validity, page protection, calling convention, Control Flow Guard, process protection, dynamic-code policy, and endpoint security can prevent or alter the operation. A function address in one process cannot always be copied blindly into another.", "Safe study focuses on contracts and evidence. Use a toy target you compile or own, avoid security products and protected processes, and prefer debugger-supported observation to executing arbitrary payloads."],
    windows: ["Process Monitor can show process and thread creation and image loads but does not provide a complete memory-write narrative. Process Explorer and VMMap can reveal private executable regions, unusual start addresses, and unsigned modules. ETW and security products add richer telemetry.", "A suspicious pattern is a relationship among access rights, new memory, protection changes, and execution, not any one API name in isolation."],
    keys: ["Cross-process operations are access-controlled", "Architecture and mitigations matter", "Evidence is a chain of related events", "Labs stay within owned toy processes"],
    apis: ["OpenProcess", "VirtualAllocEx", "WriteProcessMemory", "VirtualProtectEx", "CreateRemoteThread", "VirtualFreeEx"],
    practice: { title: "Build a defensive event model", time: "25 min", intro: "Do not implement a payload. Model what a monitor should correlate.", steps: ["List the minimum process rights associated with query, memory operation, and thread creation categories.", "Map expected observable artifacts to Process Explorer, VMMap, Process Monitor, and ETW-capable tools.", "Distinguish image-backed executable pages from private executable pages.", "Create a timeline schema for source PID, target PID, allocation, protection, start address, and module provenance.", "Write two benign explanations and two suspicious explanations that require further evidence."] },
    check: ["Why is a private executable region only a signal, not proof of malware?", ["JIT runtimes and legitimate tools can create one", "Executable memory cannot run", "VMMap invents regions", "Every process has the same pages"], 0, "Dynamic runtimes and legitimate instrumentation may use executable private memory, so provenance and behavior must be correlated."],
    sources: [["Process security rights", "https://learn.microsoft.com/windows/win32/procthread/process-security-and-access-rights"]]
  },
  {
    id: "position-independent-code", module: "hooking-injection", title: "Position-independent injected code",
    lead: "Understand why ordinary compiled code expects loader help and why movable code must avoid unrelocated absolute assumptions.",
    core: ["Compiled code can refer to globals, constants, imports, exception metadata, thread-local data, and helper functions. A normal image carries sections, imports, and relocations that the loader processes. Raw bytes placed at an arbitrary address receive none of that automatically.", "Position-independent code computes addresses relative to its current instruction or a supplied context. It still needs a defined ABI, aligned stack, valid data, and a way to locate any required functions. Architecture and compiler conventions matter."],
    mechanics: ["ASLR deliberately varies image bases. Relocation tables repair supported absolute references in PE images. Shellcode-style blobs avoid or manually resolve dependencies, which makes them compact but difficult to write, audit, and secure.", "For legitimate extensibility, a DLL or documented plugin format is usually better because the loader handles imports, relocations, TLS, unwind metadata, and initialization. Raw injected code is a poor general software architecture."],
    windows: ["Disassemblers distinguish RIP-relative x64 addressing from absolute values. PE viewers expose relocation and import directories. VMMap distinguishes image mappings from private allocations.", "This lesson is a reading and analysis exercise. It does not provide a runnable injected payload. The goal is to explain loader responsibilities and defensive artifacts."],
    keys: ["Ordinary code depends on loader services", "ASLR moves bases", "Position independence is architecture-specific", "A supported module format is safer"],
    apis: ["LoadLibraryExW", "GetProcAddress", "RtlAddFunctionTable", "FlushInstructionCache"],
    practice: { title: "Audit relocation assumptions", time: "25 min", intro: "Compare a normal DLL with a small compiler-generated function at the metadata level.", steps: ["Inspect the DLL import and base-relocation directories.", "Find one imported call and one relocation entry in a PE viewer.", "Explain what would fail if only the function's raw bytes were copied elsewhere.", "Identify unwind, data, and calling-convention dependencies.", "Propose a documented plugin or IPC alternative for the same feature."] },
    check: ["What does the PE loader normally do that raw byte copying does not?", ["Resolve imports and apply relocations", "Choose a username", "Create Registry root keys", "Increase CPU core count"], 0, "Image loading establishes dependencies and correct address-dependent data along with other runtime metadata."],
    sources: [["Base relocation block", "https://learn.microsoft.com/windows/win32/debug/pe-format#the-reloc-section-image-only"]]
  },
  {
    id: "windows-hooks-iat", module: "hooking-injection", title: "Windows hooks and IAT hooking",
    lead: "Compare documented event hooks with import-table redirection, including scope, ABI, reentrancy, and safe removal.",
    core: ["SetWindowsHookEx installs a hook procedure for a documented event family, thread, or desktop scope. Some global hook types require the procedure in a DLL whose architecture can enter target processes. Low-level hooks have timing constraints and can be removed when they misbehave.", "IAT hooking replaces an imported function's resolved address in a process table. Calls made through that slot reach the replacement, while direct calls, delayed imports, other modules' IATs, or newly resolved addresses may bypass it."],
    mechanics: ["A hook function must exactly preserve calling convention, parameters, return semantics, last-error expectations, reentrancy, and thread safety. It usually calls the original function through a saved pointer and prevents recursion.", "IAT memory may need a temporary protection change. Updates must be atomic for concurrent callers, scoped to the intended module, and fully restored before the hook code unloads."],
    windows: ["Use a local thread hook in a toy GUI you own for a documented event exercise. For IAT study, inspect tables and write pseudocode rather than patching third-party processes.", "Compatibility shims, profilers, security tools, and malware can all redirect calls. Signature, installation source, process ownership, scope, and behavior determine trust."],
    keys: ["Windows hooks target documented event flows", "IAT hooks affect indirect imports in selected modules", "ABI and reentrancy are critical", "Unhook before unloading code"],
    apis: ["SetWindowsHookExW", "CallNextHookEx", "UnhookWindowsHookEx", "VirtualProtect", "InterlockedExchangePointer"],
    practice: { title: "Design a reversible in-process hook", time: "30 min", intro: "Keep the design inside a toy program and stop at pseudocode for table modification.", steps: ["Choose one harmless imported function in a toy executable.", "Record the original IAT slot, signature, calling convention, and module scope.", "Write pseudocode for protect, atomic swap, call-through, restore, and unload ordering.", "List recursion, concurrent-call, exception, and partial-install failure cases.", "Compare this with a documented message hook and identify which is supported by Windows as an API contract."] },
    check: ["What must happen before unloading a DLL that owns hook code?", ["All hooks must be removed and active calls allowed to finish", "The target PID must be reused", "The IAT must become writable forever", "Every process must restart"], 0, "A remaining pointer to unloaded code will crash or corrupt execution when called."],
    sources: [["Hooks overview", "https://learn.microsoft.com/windows/win32/winmsg/about-hooks"]]
  },
  {
    id: "detect-injection", module: "hooking-injection", title: "Detecting injection and suspicious modules",
    lead: "Combine module provenance, memory type, thread start addresses, signatures, persistence, and event timing into an evidence-based conclusion.",
    core: ["No single indicator proves injection. Unsigned modules, unusual paths, private executable memory, threads starting outside known images, recently changed protections, cross-process handles, and unexpected startup entries become meaningful when they form a coherent timeline.", "Image-backed code has PE and file provenance. Private executable code may belong to a JIT compiler or instrumentation framework. A valid signature establishes publisher and file integrity under a trust chain, not benign behavior."],
    mechanics: ["Begin with process identity: path, signer, parent, command line, user, integrity, start time, and network role. Inventory modules and signatures. Inspect executable memory categories and thread starts. Capture image loads and persistence. Preserve hashes and timestamps.", "Different tools have blind spots and protected-process limitations. Cross-check at least two sources and state uncertainty. Do not terminate or delete a suspect artifact before evidence is preserved and authority is established."],
    windows: ["Process Explorer verifies image and module signatures and exposes threads and memory-related properties. ListDLLs gives a scriptable module list. Sigcheck inspects files and hashes. Process Monitor captures image loads and process events. Autoruns maps common persistence locations.", "A clean baseline from the same Windows version and application build is more useful than a generic list of expected DLL names."],
    keys: ["Detection needs correlated evidence", "Signed does not mean harmless", "Private executable memory has benign uses", "Preserve evidence before response"],
    apis: ["win32process.EnumProcessModules", "win32process.GetModuleFileNameEx", "VirtualQueryEx", "WinVerifyTrust"],
    practice: { title: "Investigate a controlled plugin", time: "35 min", intro: "Use a known application and a plugin you deliberately installed or a supplied lab sample.", steps: ["Capture a baseline process identity, module list, hashes, and signatures.", "Enable the controlled plugin and capture Image Load events.", "Diff module path, signer, load time, and relevant memory regions.", "Inspect thread start modules and Autoruns only if the plugin persists.", "Write a conclusion separating observed facts, likely explanation, alternative explanations, and missing evidence."] },
    check: ["What does a valid file signature establish most directly?", ["The file never behaves maliciously", "Publisher identity and integrity under the signature trust chain", "The module is loaded in every process", "The process has no private memory"], 1, "A signature helps establish provenance and whether signed bytes changed, but it is not a behavioral verdict."],
    sources: [["Process Explorer", "https://learn.microsoft.com/sysinternals/downloads/process-explorer"], ["Sigcheck", "https://learn.microsoft.com/sysinternals/downloads/sigcheck"]]
  }
];
