window.ILOVEOS_LESSON_DEPTH = {
  ...(window.ILOVEOS_LESSON_DEPTH || {}),

  "hooking-injection": {
    apis: ["SetWindowsHookExW", "LoadLibraryExW", "GetProcAddress", "win32process.EnumProcessModules", "VirtualQueryEx"],
    phases: {
      learn: ["Classify the control-flow change", "Separate injection, hooking, cooperative extension, instrumentation, and unauthorized tampering."],
      windows: ["Locate the trust boundaries", "Connect process access, modules, executable memory, event paths, and security policy."],
      investigation: ["Build a safe set of cases", "Classify evidence without modifying another process."],
      review: ["Check the mechanism model", "Test purpose, authorization, scope, persistence, redirection, and observable evidence."]
    },
    learning: [
      { title: "Injection adds code or data to an execution context", paragraphs: ["Code injection is a family of mechanisms that causes a process to run code outside its normal executable path. It can involve a loaded module, generated code, instrumentation, a debugger, or raw executable bytes. The mechanism alone does not establish whether the use is legitimate.", "A documented plugin is a cooperative extension: the host deliberately discovers, validates, loads, and calls an agreed interface. It can resemble module injection in memory, but calling every plugin injection hides the most important difference: the host consents to a supported contract."], inlineCheck: ["What most clearly distinguishes a supported plugin from unauthorized injection?", ["Every plugin is signed", "The host defines and consents to the extension contract", "Plugins never load DLLs", "Plugins run in kernel mode"], 1, "A supported host contract defines discovery, compatibility, lifecycle, and authorization."] },
      { title: "Hooking redirects an existing path", paragraphs: ["Hooking changes where a function call, message, event, or other control path goes. An IAT hook replaces one resolved import slot. A Windows message hook participates in a documented hook chain. A debugger breakpoint temporarily redirects execution through exception handling. These mechanisms have different scope and support guarantees.", "A hook often needs code already present in the process that will execute it, so some loading or injection mechanism may precede the redirection. The reverse is not required: loading a plugin does not necessarily redirect any existing operation."] },
      { title: "Technique, intent, and vulnerability are separate", paragraphs: ["Hooking and injection are not automatically vulnerabilities. Debuggers, profilers, accessibility software, compatibility systems, application performance monitoring, antimalware, game overlays, and malware can all alter execution. Authorization, user consent, target ownership, software policy, behavior, and impact determine acceptability.", "A vulnerability is a weakness that violates a security property. An attacker might exploit one to gain the rights needed for injection, but the injection API is not itself proof of the original weakness. In an investigation, distinguish how access was obtained from what was done after access was gained."] }
    ],
    visuals: [{ type: "flow", title: "Classify a runtime modification in three independent dimensions", items: [
      { meta: "Mechanism", label: "Load, generate, or redirect", detail: "What changed in memory or control flow?", linkAfter: "within" },
      { meta: "Scope", label: "Thread, module, process, desktop", detail: "Where can the change take effect?", linkAfter: "under" },
      { meta: "Authority", label: "Host contract and permissions", detail: "Who consented and what rights were used?", linkAfter: "produces" },
      { meta: "Evidence", label: "Modules, regions, threads, events", detail: "Facts that can be independently observed" }
    ], caption: "Intent cannot be inferred from one API name. Document the mechanism and authorization first." }],
    workedExamples: [{ type: "comparison", title: "Separate four commonly conflated cases", prompt: "Each case changes or observes execution, but each operates under a different contract.", columns: [
      { title: "Supported plugin", rows: [["Entry", "Host-controlled discovery"], ["Code", "Normal loaded module"], ["Redirection", "Only if the interface requires it"], ["Authority", "Documented application contract"]] },
      { title: "Debugger", rows: [["Entry", "Debug access and events"], ["Code", "Debugger plus target state"], ["Redirection", "Breakpoints and context control"], ["Authority", "Debugging authorized by the user or operator"]] },
      { title: "Unauthorized hook", rows: [["Entry", "Unapproved process access or startup mechanism"], ["Code", "Foreign module or private executable region"], ["Redirection", "Call, message, or event path"], ["Authority", "Absent or used beyond the granted scope"]] }
    ], shared: "All cases still need architecture compatibility, correct ABI, safe lifetime, and evidence-based attribution.", conclusion: "Name the mechanism and trust relationship instead of applying the label injection to every case." }],
    windowsLearning: [
      { title: "Windows enforces object and platform boundaries", paragraphs: ["Many cross-process actions begin with a process handle whose granted rights control querying, memory operations, and thread creation. Integrity levels, protected-process rules, AppContainer or capability policy, dynamic-code restrictions, Control Flow Guard, code integrity, architecture, and endpoint security can add constraints.", "Running elevated does not erase every boundary, and matching a PID does not identify a stable target by itself. Record image path, creation time, user, integrity, architecture, and protection context before interpreting access results."] },
      { title: "The visible evidence depends on the mechanism", paragraphs: ["A loaded DLL creates image-backed regions and module-list evidence. Generated or copied executable bytes can appear as MEM_PRIVATE. A hook may change an IAT slot, install a hook handle, alter a thread context, or leave only tool-specific telemetry. Process Explorer, VMMap, ListDLLs, Process Monitor, Sigcheck, Autoruns, and ETW answer different parts.", "WoW64 file-system redirection is a useful example of a system transparently intercepting behavior, but it does not prove that every compatibility mechanism uses an application-level hook. Use it to understand the outcome, and make only the implementation claims that the evidence supports."] }
    ],
    practice: {
      title: "Inspect safe modification evidence", time: "30 min", intro: "Use a PowerShell process you own and read-only tools to distinguish ordinary loaded-image evidence from private-memory evidence without creating a hook or payload.",
      expectedOutcome: "Process Explorer should show the exact kernel32.dll row and the module's loaded base address for the PowerShell process. VMMap should show image and private memory categories. Neither snapshot alone establishes unauthorized behavior.",
      safety: "Do not attach to, modify, hook, inject into, or terminate third-party processes. Use static descriptions and processes you own for read-only observation.",
      steps: [
        { action: "Open a standard PowerShell window and display its current PID and the exact trusted kernel32.dll path used in this read-only exercise.", commands: [{ label: "PowerShell", code: "$targetPid = $PID\n$modulePath = Join-Path $env:WINDIR 'System32\\kernel32.dll'\n\"Lab PowerShell PID: $targetPid\"\n\"Exact module path: $modulePath\"\nGet-Process -Id $targetPid | Select-Object Id,ProcessName,StartTime,Path" }], why: "The process instance and module path identify the targets for every later snapshot.", observe: "The console shows one live PowerShell PID, creation time, executable path, and the System32\\kernel32.dll target. An inaccessible Path field is a permission limitation; the PID and module target remain explicit." },
        { action: "Select the printed PowerShell PID in Process Explorer's top pane. Choose View > Show Lower Pane, then View > Lower Pane View > DLLs. Right-click a lower-pane column header and choose Select Columns. On the DLL tab, enable Base Address and Path, then locate the exact System32\\kernel32.dll row.", why: "Loaded-module evidence appears in the lower pane's DLL mode, not in the top pane's Start Address column.", observe: "The exact kernel32.dll row shows its loaded Base Address and Path. If access is denied or the observer cannot inspect the target architecture, the DLL snapshot is unavailable; that does not mean the module is absent." },
        { action: "Open VMMap, choose File > Select Process, select the same printed PowerShell PID, and inspect the Summary and lower Details view for Image and Private rows.", why: "Memory type and loaded-module evidence answer different questions about where memory came from.", observe: "VMMap shows image-backed and private allocations for the same process. Region counts and addresses vary by machine. If VMMap is unavailable, this classification cannot be observed." },
        { action: "In the same PowerShell, hash the exact kernel32.dll path and run Sigcheck only when sigcheck.exe is installed.", commands: [{ label: "PowerShell", code: "$modulePath = Join-Path $env:WINDIR 'System32\\kernel32.dll'\nGet-FileHash -Algorithm SHA256 -LiteralPath $modulePath\n$sigcheck = Get-Command sigcheck.exe -ErrorAction SilentlyContinue\nif ($null -eq $sigcheck) {\n  'Sigcheck unavailable; retain exact path and SHA-256.'\n} else {\n  & $sigcheck.Source -nobanner -h -a $modulePath\n}" }], why: "File identity evidence complements the live module snapshot without changing the process.", observe: "SHA-256 is always visible when the file is readable; Sigcheck adds Verified and MachineType when available. A trusted loaded image is ordinary module evidence, not proof of a hook or injection." }
      ],
      hints: [{ title: "The snapshots look suspicious", body: "A module row and private regions show where memory came from, not whether an unauthorized modification occurred. This exercise introduces no payload, redirection, or persistence event." }],
      cleanup: ["Close Process Explorer and VMMap when inspection is complete.", "The PowerShell, hash, and signature checks create no hook, process change, or startup entry."],
    },
    checks: [
      ["Is every plugin best described as unauthorized code injection?", ["Yes", "No", "Only on x64", "Only if signed"], 1, "A supported plugin follows the host's intentional extension contract."],
      ["Does one call to a cross-process API prove malicious intent?", ["Yes", "No", "Only when elevated", "Only from Python"], 1, "Consider the purpose, authorization, target, surrounding events, and behavior together."],
      ["What does a hook redirect?", ["An existing control-flow or event path", "The CPU's core count", "Every file signature", "A user's SID"], 0, "A hook changes where a call, message, event, or related operation is handled."]
    ]
  },

  "startup-code-loading": {
    apis: ["LoadLibraryExW", "SetDefaultDllDirectories", "AddDllDirectory", "GetModuleFileNameW", "win32process.EnumProcessModules", "Sigcheck"],
    phases: {
      learn: ["Trace startup loading", "Relate declared imports to search policy, architecture, loader state, and initialization."],
      windows: ["Verify the selected module", "Treat static imports, image-load events, paths, hashes, and signatures as separate sources of evidence."],
      investigation: ["Audit a controlled load", "Observe an executable you own or an explicit load without weakening system policy."],
      review: ["Check startup reasoning", "Test import resolution, search order, DllMain constraints, signatures, and safe extension."]
    },
    learning: [
      { title: "Implicit dependencies are resolved before ordinary application code", paragraphs: ["The loader maps the executable, reads its import directory, locates each required DLL under the active loader policy, maps matching-architecture images, resolves imported symbols, applies relocations where needed, and completes initialization before the executable reaches its normal entry point.", "Adding an import descriptor to a copy of an executable can therefore cause another DLL to load during startup. That is a binary-format experiment, not a recommended production plugin design. The modified file has a new hash and any original Authenticode signature normally no longer verifies."], inlineCheck: ["Where can you verify that a DLL is declared as an implicit dependency?", ["In the executable's static import directory", "In the process title", "In a Registry root", "In a CPU wait state"], 0, "The import directory records each declared DLL and imported symbol."] },
      { title: "DLL selection follows policy, not one universal directory rule", paragraphs: ["The common shortcut that a DLL in the executable directory always takes precedence is unsafe and incomplete. Known DLL handling, packaged-application rules, API sets, loaded-module reuse, Safe DLL Search Mode, altered search paths, LOAD_LIBRARY_SEARCH flags, and absolute paths can change selection.", "Use trusted application packaging, SetDefaultDllDirectories, AddDllDirectory, LoadLibraryEx with explicit search flags, and absolute paths where the contract permits. Never demonstrate search-order weakness by dropping look-alike system DLLs into ordinary application folders."] },
      { title: "DllMain is a constrained notification point", paragraphs: ["The loader can call a DLL entry point for process and thread attach or detach notifications. DllMain runs while the loader is coordinating modules and should do minimal work. Waiting on threads, loading additional libraries unpredictably, invoking complex COM or user-interface paths, or acquiring locks in conflicting order can deadlock startup.", "A supported plugin should expose an explicit initialization and shutdown contract that the host invokes after loading. This gives failures, version checks, threading, logging, and cleanup a safer place than DllMain."] }
    ],
    visuals: [{ type: "flow", title: "Follow one implicit dependency into a running process", items: [
      { meta: "On disk", label: "PE import descriptor", detail: "DLL name and imported symbols", linkAfter: "resolve under" },
      { meta: "Policy", label: "Architecture and search rules", detail: "Known DLLs, paths, flags, API sets", linkAfter: "map" },
      { meta: "Loader", label: "Image, relocations, imports", detail: "Dependency graph and constrained initialization", linkAfter: "observe" },
      { meta: "Evidence", label: "Module path, hash, signature", detail: "Static declaration plus runtime load" }
    ], caption: "A matching basename is not enough. The selected path and bytes establish which module actually entered the process." }],
    workedExamples: [{ type: "trace", title: "Audit a startup module claim", prompt: "A process unexpectedly contains helper.dll after an executable copy was modified.", steps: [
      { title: "Establish file state", action: "Hash the executable and DLL copies, record their architectures, and preserve the original hashes.", why: "Any binary modification or replacement must remain traceable.", result: "The exact lab inputs are identified and preserved." },
      { title: "Inspect the declaration", action: "Read the executable's import table and locate helper.dll and its imported symbols.", why: "Static metadata shows that the executable requests the dependency.", result: "The requested startup dependency is visible without running the executable." },
      { title: "Observe resolution", action: "Capture an Image Load event and record the complete path of the selected DLL.", why: "Search policy chooses a particular file, not just a basename.", result: "The module loaded at runtime is identified." },
      { title: "Verify provenance", action: "Compare the hash and signature status, then explain supported-plugin alternatives.", why: "A loaded module is not necessarily trusted or compatible.", result: "The report distinguishes the loading mechanism from the module's support and trustworthiness." }
    ], conclusion: "Use static imports, runtime path, and file provenance together." }],
    windowsLearning: [
      { title: "A controlled import-table scenario explains startup loading", paragraphs: ["To reason about startup code loading, keep these PE facts together: RVAs are relative to an image base, import descriptors identify dependencies, thunks hold resolved targets, architecture must match, and the loader maps file structures into memory. These are the assumptions in the scenario.", "The scenario uses disposable matching x86 artifacts and changes only copies. Do not transplant the technique into signed or third-party binaries. The learning outcome is to predict loader behavior and evidence, not to create a persistence method."] },
      { title: "Startup extension mechanisms have different support levels", paragraphs: ["Application plugin APIs, COM registration, shell extensions, debuggers, compatibility shims, and legacy AppInit behavior can all cause code to appear in processes under different contracts. Some are documented extensibility; others are compatibility features with security restrictions or poor suitability for new design.", "AppInit_DLLs is legacy, depends on user32 loading, and is restricted by code-signing and Secure Boot related policy on modern Windows. Autoruns is useful for auditing extension and persistence locations, not a checklist of safe places to add code."] }
    ],
    codeWalkthroughs: [{ title: "Load one trusted system DLL with an explicit contract", intro: "The existing starter demonstrates normal in-process loading, typed export resolution, and the corresponding release operation.", stages: [
      { title: "Choose a trusted absolute path", explanation: "The lab constructs System32\\user32.dll rather than relying on the current directory.", code: "dll_path = Path(os.environ[\"SystemRoot\"]) / \"System32\" / \"user32.dll\"\nmodule = kernel32.LoadLibraryW(str(dll_path))" },
      { title: "Resolve a typed export", explanation: "GetProcAddress returns an address, and WINFUNCTYPE defines the ABI before Python calls it.", code: "address = kernel32.GetProcAddress(module, b\"MessageBoxW\")\nmessage_box = MESSAGE_BOX_W(address)" },
      { title: "Release the owned reference", explanation: "LoadLibrary obtains a reference that this code must release with FreeLibrary after no callback can use it.", code: "try:\n    result = message_box(None, text, title, MB_OK)\nfinally:\n    if not kernel32.FreeLibrary(module):\n        raise ctypes.WinError(ctypes.get_last_error())" }
    ] }],
    practice: {
      title: "Compare declared and observed module loading", time: "40 min", intro: "Use explicit_load_lab.py and the Python process that you start.",
      download: ["downloads/explicit_load_lab.py", "explicit_load_lab.py"],
      expectedOutcome: "The process should load user32.dll from the trusted Windows system path, resolve MessageBoxW, display its module path and address, then release its explicit reference. Process Monitor and Process Explorer should agree on the selected path.",
      safety: "Do not modify system DLLs, search policy, application directories, signed third-party binaries, AppInit settings, shell extensions, or startup entries.",
      steps: [
        { action: "Open explicit_load_lab.py. Locate the code that builds the SystemRoot\\System32\\user32.dll path and defines the LoadLibraryW, GetProcAddress, GetModuleFileNameW, and FreeLibrary types. Then find the HMODULE owned by the script, the MessageBoxW prototype, the two prompts, and the final release.", why: "The lab remains controlled because it states the complete loader contract.", observe: "The supplied file does not rely on a search path or load a third-party module. It targets no remote process and changes no persistent configuration. If loading or symbol resolution fails, the script stops before constructing or calling the function pointer." },
        { action: "In Process Monitor, open Filter > Filter. Include events where Process Name is python.exe and Operation is Image Load, then clear the display and start capturing. Download explicit_load_lab.py, open PowerShell in its folder, and run the block below. Leave the script paused at 'Inspect the module and export address'.", commands: [{ label: "PowerShell", code: "$pythonExe = py -c \"import sys; print(sys.executable)\"\n\"Python executable: $pythonExe\"\npy .\\explicit_load_lab.py" }], why: "The event trace and the live module snapshot answer different parts of the investigation.", observe: "The console shows the Python executable, PID, module path, HMODULE, MessageBoxW address, and prototype. Select that PID in Process Explorer. Choose View > Show Lower Pane, then View > Lower Pane View > DLLs. Open Select Columns, add Base Address, Size, and Path, and locate the row with the exact user32.dll path printed by the script. Its address range should contain the MessageBoxW address. If access restrictions or an architecture mismatch between Process Explorer and the target prevent inspection, the snapshot is unavailable." },
        { action: "After inspecting Process Explorer, press Enter in explicit_load_lab.py and close its message box. Stop the Process Monitor capture. In CFF Explorer, open the exact Python executable printed earlier and select Import Directory in the left pane.", why: "A DLL loaded explicitly does not have to appear in the executable's static import list.", observe: "explicit_load_lab.py displays the MessageBoxW result, the IDOK value, and a message confirming that it released its reference. Process Monitor shows when the image loaded, while CFF Explorer shows the executable's static imports when available. Another dependency may keep user32.dll loaded after this script releases its reference. If CFF Explorer is unavailable, the static-import check remains unverified." }
      ],
      hints: [{ title: "The module remains visible after FreeLibrary", body: "Another dependency or earlier load may already hold a reference. FreeLibrary releases only the reference obtained by this call; it does not immediately unmap the module while other references remain." }],
      checkpoints: [{ afterStep: 2, type: "short", prompt: "Which fixed DLL does explicit_load_lab.py load from System32?", answer: "user32.dll", acceptedAnswers: ["user32"], feedback: "The supplied script builds the exact System32\\user32.dll path." }],
      cleanup: ["Close the message box and let explicit_load_lab.py release its LoadLibraryW reference with FreeLibrary.", "Close Process Monitor, Process Explorer, and CFF Explorer without changing loader or Autoruns configuration."],
    },
    checks: [
      ["Does the executable directory always win every DLL lookup?", ["Yes", "No", "Only for Python", "Only when elevated"], 1, "DLL resolution depends on the active loader policy and module class."],
      ["What normally happens to an existing Authenticode signature after PE bytes are modified?", ["It continues to verify unchanged", "Verification normally fails", "It becomes a process token", "It changes architecture"], 1, "The signed byte digest no longer matches."],
      ["Where should substantial plugin initialization occur?", ["In a host-invoked explicit function", "Only in DllMain", "Inside GetLastError", "In the IAT name field"], 0, "An explicit post-load contract avoids loader-lock constraints."]
    ]
  },

  "remote-memory-threads": {
    apis: ["win32api.OpenProcess", "win32process.VirtualAllocEx", "win32process.WriteProcessMemory", "win32process.CreateRemoteThread", "win32process.VirtualFreeEx", "VirtualQueryEx"],
    phases: {
      learn: ["Model the cross-process chain", "Track rights, remote state, architecture, execution, and cleanup without building a payload."],
      windows: ["Correlate defensive evidence", "Relate handles, private pages, protection, thread starts, mitigations, and provenance."],
      investigation: ["Survey memory provenance", "Inspect only the current lab process and build a detection timeline."],
      review: ["Check remote-operation reasoning", "Test access, address spaces, architecture, mitigations, evidence, and uncertainty."]
    },
    learning: [
      { title: "Cross-process memory operations depend on object permissions", paragraphs: ["A caller first identifies a particular target process and opens a handle with the permissions required for the next operation. Reserving or committing memory in another process requires memory-operation access. Writing requires both write and operation access. Creating a remote thread requires its documented set of permissions and a valid start address in the target.", "Requesting PROCESS_ALL_ACCESS obscures the permissions actually required and can fail where a narrower diagnostic request succeeds. A defensive timeline should record the requested and granted access, source and target identities, allocation address and type, write size, protection, execution start, exit, release, and handle close."], inlineCheck: ["Why record the requested process access mask?", ["It reveals which permissions the caller attempted to obtain", "It determines the target username", "It replaces the PID", "It proves the payload bytes"], 0, "The access mask shows which security-sensitive operations the caller attempted to authorize."] },
      { title: "An address belongs to one process context", paragraphs: ["VirtualAllocEx returns an address meaningful in the target process. A local pointer to a buffer, function, or DLL does not become a remote pointer simply because both values fit in an integer. ASLR, architecture, loader state, and module versions affect addresses.", "CreateRemoteThread expects a start routine that follows the target ABI and remains valid for the thread lifetime. Copying a local function address or raw Python callback is not a valid cross-process design. This course stops at the resource and evidence model rather than constructing executable remote bytes."] },
      { title: "Modern mitigations make the simplified sequence incomplete", paragraphs: ["DEP page protections, Control Flow Guard, Arbitrary Code Guard or dynamic-code policy, process protection, code integrity, architecture mismatch, and endpoint security can block or flag steps. A writable allocation is not automatically executable, and changing protection creates additional evidence and risk.", "A debugger or profiler may perform legitimate cross-process reads, writes, and thread control. Before calling activity suspicious, consider the source's authorization, its relationship to the target, memory provenance, start address, signature and module context, frequency, and behavior."] }
    ],
    visuals: [{ type: "flow", title: "Correlate the remote-operation chain without executing it", items: [
      { meta: "Source", label: "Process handle and granted rights", detail: "Stable source and target identity", linkAfter: "changes" },
      { meta: "Target memory", label: "Allocation, bytes, protection", detail: "Address, size, MEM_PRIVATE or image", linkAfter: "may lead to" },
      { meta: "Execution", label: "Thread start and lifetime", detail: "Start address, owner process, exit", linkAfter: "evaluate with" },
      { meta: "Context", label: "Mitigations and provenance", detail: "Architecture, signer, purpose, policy" }
    ], caption: "No single box proves injection. The relationship and sequence make the event meaningful to an investigator." }],
    workedExamples: [{ type: "contract", title: "Track the resources involved in each API call", prompt: "A monitoring rule observes a source process accessing a controlled test target.", steps: [
      { title: "OpenProcess", action: "The source obtains one process handle with a recorded access mask.", why: "The handle grants the access used by later calls.", result: "The source owns a local handle, but no bytes in the target have changed yet." },
      { title: "VirtualAllocEx", action: "Target gains a region with base, size, state, type, and protection.", why: "Remote address and allocation ownership belong to the target context.", result: "Target memory state changes." },
      { title: "Write and protect", action: "The bytes and, possibly, the page protection change through operations with separate permissions and results.", why: "Write success, byte count, and executable state are distinct facts.", result: "The content change and memory protection each produce their own evidence." },
      { title: "Execution and cleanup", action: "Record the outcome of the start mechanism, completion wait, remote-memory release, and local handle close.", why: "Partial failure can leave a thread, region, or handle alive.", result: "The complete lifecycle can be audited." }
    ], conclusion: "Model every state transition and owner, including branches where a later step never occurs." }],
    windowsLearning: [
      { title: "Memory type improves provenance analysis", paragraphs: ["MEM_IMAGE regions come from mapped executable images and can be tied to a file and PE sections. MEM_MAPPED regions have section backing but are not necessarily executable images. MEM_PRIVATE regions belong to private allocations. Executable MEM_PRIVATE pages are noteworthy but common in JIT runtimes and some instrumentation.", "VMMap provides a rich classification. VirtualQueryEx provides region state, protection, type, base, allocation base, and size. In Process Explorer, select a process in the top pane and open Properties > Threads before selecting a TID and reading that thread's Start Address; symbols may remain unresolved. For a loaded module Base Address, select the process, choose View > Show Lower Pane and View > Lower Pane View > DLLs, right-click a lower-pane header, choose Select Columns, open DLL, enable Base Address, and locate the exact module path row. These are distinct views."] },
      { title: "Tool coverage has gaps", paragraphs: ["Process Monitor is useful for the timing of process, thread, image-load, file, and Registry activity, but it does not capture every remote memory write. Process Explorer and VMMap provide snapshots. ETW providers, Windows security products, debuggers, or purpose-built telemetry may expose additional access and memory events.", "A report should state what was not captured. Absence from Process Monitor does not prove that no cross-process write occurred, and a module list does not expose raw private executable code."] }
    ],
    practice: {
      title: "Inspect memory provenance in the current process", time: "35 min", intro: "memory_provenance_lab.py queries only its own address space and never writes to memory or changes its protection.",
      download: ["downloads/memory_provenance_lab.py", "memory_provenance_lab.py"],
      expectedOutcome: "The report should count committed MEM_IMAGE, MEM_MAPPED, and MEM_PRIVATE regions, list executable regions, and show that image-backed and private executable memory are different provenance categories. Addresses and counts vary by Python build.",
      safety: "memory_provenance_lab.py exposes no external PID argument and performs no allocation, write, protection change, thread creation, or code execution.",
      steps: [
        { action: "Open memory_provenance_lab.py and locate its SYSTEM_INFO and MEMORY_BASIC_INFORMATION layouts, GetNativeSystemInfo/VirtualQueryEx prototypes, GetCurrentProcess pseudo-handle, SIZE_T fields, forward-progress guard, and --no-pause option.", why: "Pointer width, structure layout, and zero-return handling are core ABI requirements.", observe: "The supplied file has no PID argument, OpenProcess, allocation, write, protection change, thread creation, or executable payload." },
        { action: "Download memory_provenance_lab.py, open PowerShell in its folder, run it without --no-pause, and leave it at 'Compare this process with VMMap'.", commands: [{ label: "PowerShell", code: "py .\\memory_provenance_lab.py" }], why: "The pause keeps this particular self-querying process alive for read-only comparison.", observe: "The report shows the PID and pointer width; committed region and byte totals for MEM_IMAGE, MEM_MAPPED, MEM_PRIVATE, and unknown types; the number of committed executable regions; and rows containing the base, size, type, and protection. If VirtualQueryEx returns zero, the script advances by one page. It treats a zero-size region or one that does not advance as a fatal error." },
        { action: "While memory_provenance_lab.py remains paused, open VMMap, choose File > Select Process, select the printed PID, and inspect matching address ranges in the Summary and lower Details views.", why: "VirtualQueryEx and VMMap expose related evidence at different levels.", observe: "First compare the base addresses and sizes of representative executable MEM_IMAGE regions and any executable MEM_PRIVATE regions. Then compare their labels and totals. If the process has exited, its architecture is incompatible with the observer, or VMMap cannot access it, the view is unavailable; do not substitute another process." }
      ],
      hints: [{ title: "The region totals differ from VMMap", body: "VirtualQuery reports raw regions, while VMMap groups and labels allocations using additional rules. Compare address ranges and types before comparing category totals." }],
      cleanup: ["Press Enter to let the self-querying process exit.", "No remote process or persistent state is changed."],
    },
    checks: [
      ["Is an executable MEM_PRIVATE region conclusive proof of malware?", ["Yes", "No", "Only on x86", "Only in Python"], 1, "JIT engines and legitimate instrumentation can create private executable memory."],
      ["Can a local function pointer be assumed valid in another process?", ["Yes", "No", "Only if decimal", "Only with a PID"], 1, "Addresses are interpreted within a process's own mappings and ABI context."],
      ["Does Process Monitor capture every remote memory write?", ["Yes", "No", "Only for DLLs", "Only when signed"], 1, "Its standard events do not provide a complete audit trail of memory writes."]
    ]
  },

  "position-independent-code": {
    apis: ["LoadLibraryExW", "GetProcAddress", "RtlAddFunctionTable", "FlushInstructionCache", "VirtualQueryEx", "PE base relocations"],
    phases: {
      learn: ["Account for loader services", "Trace addresses, imports, relocations, data, TLS, unwind metadata, and ABI assumptions."],
      windows: ["Read the binary evidence", "Use PE directories and disassembly to distinguish supported images from raw code."],
      investigation: ["Audit relocation assumptions", "Inspect a PE file you own and stop before copying executable bytes."],
      review: ["Check position independence", "Test relative addressing, dependencies, architecture, metadata, and safer alternatives."]
    },
    learning: [
      { title: "Ordinary compiled code depends on more than its instruction bytes", paragraphs: ["A compiled function can refer to global variables, string literals, import thunks, helper routines, security cookies, thread-local storage, exception or unwind metadata, and alignment assumptions. A PE image supplies sections and directories that let the loader establish those dependencies.", "Copying the bytes of one function to another address omits everything not embedded in that byte range. Even if the first instruction runs, a later relative branch, data reference, stack-unwind event, or imported call can fail."], inlineCheck: ["What does a raw function-byte copy normally omit?", ["Only its source filename", "External data and loader-managed dependencies", "The target PID only", "All arithmetic instructions"], 1, "The function can depend on state and metadata outside the copied range."] },
      { title: "Position independence depends on the architecture and representation", paragraphs: ["Position-independent code calculates needed addresses relative to the current instruction, a supplied context, or another relocatable base. x64 commonly uses RIP-relative addressing, and self-contained literals can be stored near the code and reached relatively. The claim that position-independent code cannot use strings is therefore too absolute.", "The real restriction is an uncorrected assumption about location. A literal, table, or helper can work if it is included in the self-contained layout and addressed through a valid relative scheme. External globals and imports still need a resolution strategy."] },
      { title: "The loader supplies relocation and runtime metadata", paragraphs: ["When an image cannot use its preferred base, base-relocation entries identify address-dependent locations the loader can adjust. The import directory supplies names and thunks for external resolution. TLS directories, load configuration, exception tables, and section protections provide other runtime contracts.", "On x64, dynamically generated functions that can participate in stack unwinding may need function-table registration such as RtlAddFunctionTable. After producing or modifying native instructions, the documented contract may require FlushInstructionCache before execution. These details reinforce why a normal DLL or supported plugin is safer than raw code placement."] }
    ],
    visuals: [{ type: "layers", title: "What a normal PE image brings beyond raw instructions", items: [
      { meta: "Code and data", label: "Sections with relative layout", detail: "Instructions, constants, globals, protections", linkAfter: "described by" },
      { meta: "Resolution", label: "Imports and relocations", detail: "External symbols and base-dependent fields", linkAfter: "supported by" },
      { meta: "Runtime", label: "TLS, unwind, load configuration", detail: "Thread data, exceptions, mitigations", linkAfter: "establish" },
      { meta: "Execution", label: "Valid ABI and lifetime", detail: "Stack, calling convention, code ownership" }
    ], caption: "A byte sequence contains instructions. An image supplies the broader contract needed to run them reliably." }],
    workedExamples: [{ type: "comparison", title: "Compare a loaded DLL and a copied function", prompt: "Both contain native instructions, but only one follows the image-loading contract.", columns: [
      { title: "Loaded DLL", rows: [["Addressing", "Relocations applied when needed"], ["Imports", "Resolved into IAT"], ["Data", "Mapped sections"], ["Exceptions", "Image unwind metadata"], ["Lifecycle", "Loader reference and explicit exports"]] },
      { title: "Copied bytes", rows: [["Addressing", "Only self-contained calculations remain valid"], ["Imports", "No automatic resolution"], ["Data", "Only data deliberately included in the copied layout"], ["Exceptions", "No automatic registration"], ["Lifecycle", "Custom ownership and synchronization"]] }
    ], shared: "Both must match architecture, calling convention, stack alignment, security policy, and target lifetime.", conclusion: "Prefer the standard image contract unless raw generated code is an intentional, reviewed runtime requirement." }],
    windowsLearning: [
      { title: "Static PE inspection reveals loader expectations", paragraphs: ["Inspect the import, base-relocation, exception, TLS, and load-configuration directories. Translate each RVA through the section table and check file bounds. An absent directory can be valid, but its absence changes what the image can safely rely on.", "A disassembler can identify RIP-relative x64 operands and absolute embedded addresses. One instruction view is not enough to prove a function is movable, because data and control dependencies can extend beyond the visible block."] },
      { title: "Memory type supports the distinction", paragraphs: ["A normally loaded DLL appears in MEM_IMAGE regions whose ranges and protections derive from PE sections. Raw generated or copied code generally appears in MEM_PRIVATE or a non-image mapping. VMMap and VirtualQuery expose this difference, though packers, manual loaders, and advanced runtimes complicate interpretation.", "Do not turn this lesson into a payload-construction exercise. The goal is to predict why copied code might fail, identify the evidence it leaves, and choose an alternative supported by the loader."] }
    ],
    practice: {
      title: "Audit one PE file's assumptions about moving code", time: "35 min", intro: "Use pe_inspector_lab.py on the current Python executable and display metadata only.",
      download: ["downloads/pe_inspector_lab.py", "pe_inspector_lab.py"],
      expectedOutcome: "pe_inspector_lab.py should identify architecture, preferred image base, entry RVA, sections, and permissions. CFF Explorer should expose the same file's Import Directory and Relocation Directory without any byte copying or execution.",
      steps: [
        { action: "Download pe_inspector_lab.py, open PowerShell in its folder, locate the current Python executable, and parse that exact PE file without modifying it.", commands: [{ label: "PowerShell", code: "$targetPe = py -c \"import sys; print(sys.executable)\"\nif (-not (Test-Path -LiteralPath $targetPe -PathType Leaf)) { throw \"Python executable not found: $targetPe\" }\npy .\\pe_inspector_lab.py $targetPe" }], why: "Any claim about where code can run must begin with the layout of a specific image.", observe: "The report shows the resolved file, Machine value, Optional Header Magic, preferred ImageBase, AddressOfEntryPoint RVA, alignments, image and header sizes, and each section's RVA, raw-file location, and protections. File offsets, RVAs, and runtime virtual addresses remain distinct." },
        { action: "Open the printed Python executable in CFF Explorer with File > Open, select Import Directory in the left tree, and choose one named or ordinal import row.", why: "Raw copied bytes do not receive automatic external symbol resolution.", observe: "The row shows the source DLL, symbol or ordinal, thunk or IAT field, and importing module. If CFF Explorer or a field is unavailable, the cross-check is unavailable; do not invent an address." },
        { action: "In the same read-only CFF Explorer session, select Relocation Directory in the left tree and inspect one relocation block, or confirm that the directory is absent.", why: "Relocation data identifies the base-dependent locations that the loader knows how to adjust.", observe: "Available rows show the block RVA, type, and entry count. An absent directory can constrain rebasing or reflect architecture and build choices. Do not save file changes or copy or execute any function bytes." }
      ],
      hints: [{ title: "The function uses RIP-relative addressing", body: "That helps position independence for the referenced relative target, but you must still prove the target bytes move with the function and all other dependencies remain valid." }],
      cleanup: ["pe_inspector_lab.py performs static file reads only.", "Close CFF Explorer or Ghidra without saving modifications to the inspected file."],
    },
    checks: [
      ["Can position-independent code contain a nearby string literal?", ["Never", "Yes, if the layout and addressing remain position independent", "Only in kernel mode", "Only in decimal"], 1, "Self-contained data can be reached through valid relative addressing."],
      ["What do base relocations identify?", ["Address-dependent image locations to adjust", "User account groups", "Pipe message boundaries", "Thread priorities"], 0, "They let the loader repair supported absolute assumptions after rebasing."],
      ["Why is a DLL normally safer than raw copied code for extension?", ["The loader establishes imports, relocations, metadata, and lifecycle", "DLLs cannot fail", "DLLs always run as SYSTEM", "Raw code has no instructions"], 0, "The image format and loader provide a defined runtime contract."]
    ]
  },

  "windows-hooks-iat": {
    apis: ["SetWindowsHookExW", "CallNextHookEx", "UnhookWindowsHookEx", "VirtualProtect", "InterlockedExchangePointer", "PE import address table"],
    phases: {
      learn: ["Compare two redirection mechanisms", "Separate documented event-hook scope from per-module import-slot replacement."],
      windows: ["Protect ABI and lifecycle", "Reason about chains, reentrancy, concurrency, architecture, restoration, and unload."],
      investigation: ["Inspect without patching", "Map the import table of a file you own and design only reversible pseudocode."],
      review: ["Check hook reasoning", "Test scope, bypass paths, callback lifetime, recursion, atomicity, and removal."]
    },
    learning: [
      { title: "Windows hooks participate in documented event paths", paragraphs: ["SetWindowsHookEx installs a hook procedure for a selected hook type and either a specific thread or a desktop-wide scope when thread ID is zero. Scope, DLL requirements, architecture interaction, process boundaries, desktop boundaries, and event delivery depend on the chosen hook type.", "Saying that a hook 'injects into all processes' is too broad. Some hook procedures execute in the installing process, some require a DLL for cross-process use, and low-level hooks are delivered differently. Only processes and threads that receive relevant events within the hook's scope participate. Existing eligible threads can be affected; they do not always have to be created after the hook is installed."], inlineCheck: ["Which factors determine the scope of a SetWindowsHookEx hook?", ["The hook type, thread ID, desktop, architecture, and callback placement", "Only the DLL filename", "The current file offset", "The user's wallpaper"], 0, "Hook scope depends on several documented settings; it does not simply mean all processes."] },
      { title: "Hook procedures must preserve the hook chain", paragraphs: ["A hook callback must use the exact signature and calling convention, finish within the hook type's timing expectations, handle reentrancy and exceptions, and call CallNextHookEx when the contract requires the rest of the chain to run. Blocking or silently consuming events can destabilize unrelated applications.", "The callback and its containing DLL must remain loaded while the hook can call it. UnhookWindowsHookEx prevents future delivery, but a callback already in progress may still be finishing. Coordinate shutdown before unloading the code."] },
      { title: "An IAT hook changes selected indirect calls", paragraphs: ["After the loader resolves imports, an IAT slot contains a target function address. Replacing that pointer redirects calls from that module that pass through that slot. It does not automatically affect direct syscalls, statically linked code, delay-load tables not yet handled, GetProcAddress results, another module's IAT, or a caller that cached the original pointer.", "Correct code needs the exact function ABI, a saved original pointer, recursion control, thread-safe installation, temporary page-protection handling where necessary, and a safe restoration process. Before unloading the hook code, it must also ensure that no active call can enter it."] }
    ],
    visuals: [{ type: "flow", title: "Follow an imported call before and after IAT redirection", items: [
      { meta: "Caller module", label: "call [IAT slot]", detail: "One module's indirect import path", linkAfter: "normally points to" },
      { meta: "Original", label: "Imported function", detail: "Resolved export in dependency", linkAfter: "hook installation redirects" },
      { meta: "Replacement", label: "ABI-compatible hook", detail: "May observe, modify, and call original", linkAfter: "uninstall restores" },
      { meta: "Lifecycle", label: "Original pointer and protection", detail: "Atomic swap, wait for active calls, then unload" }
    ], caption: "Only calls that consult the modified slot are redirected. This bounded scope is central to both debugging and detection." }],
    workedExamples: [{ type: "comparison", title: "Windows event hook versus IAT hook", prompt: "Both redirect behavior, but their contracts and bypasses differ.", columns: [
      { title: "SetWindowsHookEx", rows: [["Target", "Documented event family"], ["Scope", "Thread or desktop-dependent"], ["Chain", "CallNextHookEx semantics"], ["Installation", "OS-managed hook handle"], ["Removal", "Unhook plus callback lifetime"]] },
      { title: "IAT replacement", rows: [["Target", "One imported function slot"], ["Scope", "Selected importing module"], ["Chain", "Saved original pointer by design"], ["Installation", "Table pointer change"], ["Removal", "Restore pointer, drain calls, unload"]] }
    ], shared: "Both require an exact ABI, architecture compatibility, reentrancy planning, thread safety, bounded work, and authorization.", conclusion: "Name the mechanism after the point it actually redirects, rather than relying on the general word hook." }],
    windowsLearning: [
      { title: "Callback ABI mistakes can corrupt a process", paragraphs: ["A wrong parameter type, return type, calling convention, or callback lifetime can corrupt registers or stack state, return invalid results, or jump into freed Python callback storage. ctypes.WINFUNCTYPE creates a native callback wrapper, but the Python object must remain strongly referenced while Windows may call it.", "Python callbacks are a poor fit for broad, timing-sensitive global hooks. The course does not install them. Study the signature, event chain, and lifecycle through documentation and pseudocode."] },
      { title: "Kernel table hooking is outside this lab", paragraphs: ["Historical SSDT or syscall-table hooking requires kernel code and conflicts with modern kernel integrity protections, architecture changes, driver signing, PatchGuard, virtualization-based security, and supportability. It is not simply an IAT hook with higher permissions.", "Use documented tracing, filtering, minifilter, callback, ETW, and security interfaces for legitimate kernel observation. The course treats SSDT hooking as historical vocabulary and a defensive indicator, not an implementation practice."] }
    ],
    practice: {
      title: "Inspect possible redirection points in one executable's import table", time: "40 min", intro: "pe_imports_lab.py reads import descriptors and IAT RVAs from a PE file you own without loading or modifying it.",
      download: ["downloads/pe_imports_lab.py", "pe_imports_lab.py"],
      expectedOutcome: "pe_imports_lab.py should list imported DLLs and named or ordinal imports with IAT RVAs. CFF Explorer should show the same fixed import row while the supplied parser remains static and read-only.",
      safety: "Do not patch a running process or file. Work on static files you own and keep the exercise at inspection and pseudocode.",
      steps: [
        { action: "Download pe_imports_lab.py, open PowerShell in its folder, locate the current Python executable, and run the static import parser on that exact file.", commands: [{ label: "PowerShell", code: "$targetPe = py -c \"import sys; print(sys.executable)\"\nif (-not (Test-Path -LiteralPath $targetPe -PathType Leaf)) { throw \"Python executable not found: $targetPe\" }\npy .\\pe_imports_lab.py $targetPe" }], why: "Static metadata identifies the imports of that particular module without loading or editing the file.", observe: "The report shows the resolved file, machine type, import-directory RVA and size, each DLL, and every named or ordinal import with its IAT RVA. 'no ordinary import descriptors' is also a valid result." },
        { action: "In CFF Explorer, choose File > Open, select the same Python executable printed by the PowerShell block, select Import Directory in the left tree, and choose one harmless row printed by pe_imports_lab.py.", why: "IAT metadata belongs to one importing module and one specific slot.", observe: "The DLL, symbol or ordinal, and IAT RVA agree across both tools. GetProcAddress, another module's table, direct calls, and delay-load resolution do not use that ordinary slot. If CFF Explorer is unavailable, the cross-check is limited." },
        { action: "Open pe_imports_lab.py and locate its import-directory bounds, descriptor terminator, thunk-width selection, ordinal flag, name-reading logic, and IAT RVA calculation.", why: "These parser boundaries define what the static result proves; they do not introduce any modification mechanism.", observe: "The supplied file reads PE bytes and prints metadata only. It contains no OpenProcess, VirtualProtect, pointer swap, hook installation, payload, or memory write." }
      ],
      hints: [{ title: "The report shows an ordinal import", body: "An ordinal identifies an export by number rather than name. Retain the ordinal and source DLL instead of inventing a symbol name." }],
      cleanup: ["pe_imports_lab.py performs static file reads only.", "Close CFF Explorer without saving modifications to the inspected file."],
    },
    checks: [
      ["Does changing one module's IAT slot redirect every process call to that API?", ["Yes", "No", "Only on x64", "Only if signed"], 1, "Only calls that use that selected slot are redirected."],
      ["What must happen before hook code is unloaded?", ["Restore redirection and let active callbacks finish", "Make the IAT writable forever", "Close every system process", "Delete the original DLL"], 0, "No remaining call path may point to code that has been unloaded."],
      ["Is SSDT patching a supported user-mode WinAPI hook?", ["Yes", "No", "Only through ctypes", "Only with Process Explorer"], 1, "It is kernel tampering and outside documented user-mode hook contracts."]
    ]
  },

  "detect-injection": {
    apis: ["win32process.EnumProcessModules", "win32process.GetModuleFileNameEx", "VirtualQueryEx", "WinVerifyTrust", "Process Explorer", "ListDLLs", "Sigcheck"],
    phases: {
      learn: ["Build a layered evidence model", "Combine identity, modules, memory, threads, persistence, events, trust, and baseline."],
      windows: ["Correlate complementary tools", "Use snapshots and timelines to reach a conclusion that reflects the limits and uncertainty in the evidence."],
      investigation: ["Investigate a controlled module change", "Establish a baseline for a process you own, compare it, and complete the full-course synthesis."],
      review: ["Check defensive reasoning", "Test provenance, signatures, false positives, tool limits, response, and course-wide integration."]
    },
    learning: [
      { title: "Start with stable process identity", paragraphs: ["Record image path, file hash and signer, PID and creation time, parent, command line, user, integrity, architecture, protection level, start time, and expected role. PID alone is reusable and a familiar process name can be copied by unrelated software.", "Define the question before collecting every available field. Are you explaining an unexpected module, a private executable region, an unusual thread start, a startup entry, or a cross-process relationship? The question determines the evidence sequence."], inlineCheck: ["Why pair PID with creation time and image path?", ["PIDs can be reused and names can be imitated", "It changes the DACL", "It creates an IAT", "It guarantees malware"], 0, "This combination identifies the observed process instance more reliably."] },
      { title: "Indicators become meaningful through correlation", paragraphs: ["An unsigned module, unusual path, executable private page, thread starting outside a known image, writable-executable protection, changed IAT slot, suspicious cross-process handle, or unexpected startup entry can each have benign explanations. Several connected events with inconsistent provenance provide stronger evidence that the activity is suspicious.", "A valid signature identifies the publisher and shows whether the signed bytes have changed under the trust chain. It does not prove that the program behaves safely. An unsigned file is not automatically malicious, especially during local development, but its publisher is harder to verify."] },
      { title: "Baseline and time order reduce guesswork", paragraphs: ["Compare the same application version, configuration, architecture, and workload before and after the controlled change. A generic list of Windows DLLs is less useful than a known-good baseline for that exact program.", "Record when the parent started, when a module loaded, when memory protection changed, when a thread began, and when persistence appeared. Causality still requires care, but ordered evidence is stronger than an unordered screenshot."] }
    ],
    visuals: [{ type: "flow", title: "Move from anomaly to evidence-based conclusion", items: [
      { meta: "Identity", label: "Exact process instance", detail: "Path, hash, signer, parent, token, time", linkAfter: "compare" },
      { meta: "Execution", label: "Modules, regions, threads", detail: "Image or private provenance, start addresses", linkAfter: "correlate" },
      { meta: "Timeline", label: "Loads, opens, persistence, network", detail: "Source, target, result, sequence", linkAfter: "conclude" },
      { meta: "Report", label: "Facts, inference, alternatives", detail: "Confidence, gaps, safe next action" }
    ], caption: "An investigation report should remain reproducible even when a reader disagrees with its interpretation." }],
    workedExamples: [{ type: "decision", title: "Triage an unexpected module", prompt: "Process Explorer shows an unfamiliar DLL in a normal desktop application.", steps: [
      { title: "Verify the process", action: "Record the exact process identity and expected application build.", why: "A module conclusion is meaningless if the wrong process instance was inspected.", result: "The correct process instance is fixed as the subject of the investigation." },
      { title: "Verify the module", action: "Record the full path, hash, signature, company metadata, architecture, and file timestamps.", why: "A basename does not establish provenance.", result: "The module can now be identified independently of the process snapshot." },
      { title: "Find the load path", action: "Compare the baseline and examine Image Load timing, then inspect the parent, plugin, installer, or startup context.", why: "The reason it entered the process matters more than unfamiliarity.", result: "Each possible cause is tied to specific evidence." },
      { title: "Qualify the conclusion", action: "State the facts, likely explanation, alternatives, missing telemetry, and a proportionate next action.", why: "A sound investigation acknowledges uncertainty and preserves evidence.", result: "Do not take destructive action before establishing both authorization and sufficient evidence." }
    ], conclusion: "Unexpected is a question to investigate, not a verdict." }],
    windowsLearning: [
      { title: "Use each Sysinternals tool for its strongest question", paragraphs: ["Process Explorer provides process identity, token context, modules, signature checks, handles, threads, and start addresses. ListDLLs creates a scriptable module inventory. Sigcheck inspects file hashes, versions, and signatures. VMMap classifies memory. Process Monitor captures process, thread, image, file, and Registry timing. Autoruns maps common persistence locations.", "A module can unload before a snapshot, a protected process can restrict inspection, signature verification can depend on network and trust configuration, and Process Monitor can miss pre-capture events. Record the tool version, privilege level, filters, capture window, and any failed operations."] },
      { title: "Preserve before responding", paragraphs: ["Hash relevant files, save focused exports or screenshots, record timestamps and the system's time zone, and preserve the connection between the process, module, path, and event. Do not upload sensitive files to third-party services without authorization and policy review.", "Do not delete, terminate, quarantine, change ACLs, disable security controls, or remove persistence simply because a lab indicator looks suspicious. In a real environment, follow incident-response authorization and evidence-preservation procedures."] }
    ],
    practice: {
      title: "Baseline the current Python process", time: "45 min", intro: "module_baseline_lab.py enumerates only its own loaded modules, saves the results as JSON, and compares runs that you control.",
      download: ["downloads/module_baseline_lab.py", "module_baseline_lab.py"],
      expectedOutcome: "The report should include the PID, executable, architecture, module paths, hashes where readable, and a consistent JSON format. A comparison should identify added modules, removed modules, and changed hashes for the same path without declaring them malicious.",
      safety: "module_baseline_lab.py inspects only its own process and performs no hook, injection, persistence change, or external process open. The controlled comparison adds only Python's standard sqlite3 extension to its own process.",
      steps: [
        { action: "Download module_baseline_lab.py and open two PowerShell windows in its folder. Start the baseline command first, using the exact JSON filename below, and leave it at the comparison prompt. Enter its printed PID in the observer window.", commands: [{ label: "Baseline PowerShell", code: "py .\\module_baseline_lab.py --output .\\iloveos-module-baseline.json" }, { label: "Observer PowerShell", code: "$targetPid = [int](Read-Host 'Enter the PID printed by module_baseline_lab.py')\nGet-Process -Id $targetPid -ErrorAction Stop | Select-Object Id,ProcessName,StartTime,Path\n$listdlls = Get-Command listdlls.exe -ErrorAction SilentlyContinue\nif ($null -eq $listdlls) {\n  'ListDLLs unavailable; retain module_baseline_lab.py and Process Explorer evidence.'\n} else {\n  & $listdlls.Source -accepteula $targetPid\n}" }], why: "Independent snapshots reveal differences in names, normalized paths, timing, and access limitations.", observe: "module_baseline_lab.py shows the PID, executable, pointer width, loaded-module count, abbreviated base/path/hash data, output path, and capture time. Select the same PID in Process Explorer. Choose View > Show Lower Pane, then View > Lower Pane View > DLLs. Open Select Columns, add Base Address and Path, and locate an exact module path row from the report. If ListDLLs or Process Explorer cannot inspect the process, that snapshot is unavailable." },
        { action: "Press Enter in the baseline window to close the first process. In Process Monitor, include events where Process Name is python.exe and Operation is Image Load. Clear the display and start a new capture. Run the controlled comparison command below, which imports sqlite3 before creating the second report, and leave it paused.", commands: [{ label: "Controlled comparison PowerShell", code: "py -c \"import runpy, sqlite3, sys; sys.argv=['module_baseline_lab.py','--compare','.\\\\iloveos-module-baseline.json','--output','.\\\\iloveos-module-with-sqlite.json']; runpy.run_path(r'.\\\\module_baseline_lab.py', run_name='__main__')\"" }], why: "A known standard-library extension tests the added, removed, and changed-hash output without injecting code into another process.", observe: "The second report shows the new PID and added modules such as _sqlite3.pyd and its dependencies, along with removed modules, changed hashes, the output path, and Image Load events. If sqlite3 was already loaded or is unavailable, this controlled comparison cannot be made; do not substitute an unknown extension." },
        { action: "Keep the comparison process paused. In VMMap, select its printed PID, expand the Image rows, and find the exact _sqlite3.pyd path. Read its address and size. In Process Explorer, select the same PID and show the lower pane in DLL mode. Add the Base Address and Path columns, then find the _sqlite3.pyd path listed in iloveos-module-with-sqlite.json. Use the block below to hash that exact file and, when Sigcheck is available, check its signature.", commands: [{ label: "Evidence PowerShell", code: "$report = Get-Content -Raw -LiteralPath .\\iloveos-module-with-sqlite.json | ConvertFrom-Json\n$modulePath = $report.modules.path | Where-Object { $_ -match '\\\\_sqlite3\\.pyd$' } | Select-Object -First 1\nif ($null -eq $modulePath) { 'Controlled _sqlite3.pyd module unavailable in this report.' } else {\n  Get-FileHash -Algorithm SHA256 -LiteralPath $modulePath\n  $sigcheck = Get-Command sigcheck.exe -ErrorAction SilentlyContinue\n  if ($null -eq $sigcheck) { 'Sigcheck unavailable; retain SHA-256 and module path.' } else { & $sigcheck.Source -nobanner -h -a $modulePath }\n}" }], why: "The module inventory, memory type, load time, hash, and signer each answer a different question.", observe: "VMMap shows the matching image region, Process Explorer shows its module path and base address, and Process Monitor shows when it loaded. The PowerShell block reports SHA-256, while Sigcheck adds verification and machine type when installed. If VMMap, Sigcheck, or _sqlite3.pyd is missing, that part of the evidence remains unavailable." },
        { action: "Press Enter in the controlled comparison process so it exits, stop Process Monitor with File > Capture Events, then remove only the two JSON files created by module_baseline_lab.py.", commands: [{ label: "PowerShell cleanup", code: "$baselineFiles = @(\n  (Join-Path $PWD 'iloveos-module-baseline.json'),\n  (Join-Path $PWD 'iloveos-module-with-sqlite.json')\n)\nforeach ($file in $baselineFiles) {\n  if (Test-Path -LiteralPath $file -PathType Leaf) { Remove-Item -LiteralPath $file }\n}\n$baselineFiles | ForEach-Object { \"Present after cleanup: $_ = $(Test-Path -LiteralPath $_)\" }" }], why: "The cleanup names the two output files explicitly and runs only after all snapshot evidence has been collected.", observe: "Both Python processes you started exit, and each cleanup line ends with False. No module file, signed binary, or unrelated baseline is deleted." }
      ],
      hints: [{ title: "Two inventories differ unexpectedly", body: "Align capture time, process instance, architecture, path normalization, access level, and modules that loaded or unloaded between snapshots before interpreting the difference." }],
      cleanup: ["Close the Python process you started and the read-only inspection tools.", "The supplied cleanup block deletes only iloveos-module-baseline.json and iloveos-module-with-sqlite.json."],
    },
    checks: [
      ["What can a valid Authenticode signature tell you?", ["Who signed the file and whether its signed bytes have changed", "That the file will always behave safely", "That the process has no private memory", "That every administrator can access it"], 0, "A signature provides publisher and integrity information. Program behavior still needs a separate evaluation."],
      ["Should an unfamiliar module be deleted before its path and hash are preserved?", ["Yes", "No", "Only if unsigned", "Only from System32"], 1, "Preserve the evidence and confirm your authorization before responding."],
      ["Which conclusion is strongest?", ["Private executable memory always means malware", "A conclusion based on several correlated facts that also states alternatives and evidence gaps", "A familiar filename is trusted", "One clean snapshot proves no injection"], 1, "Correlation, reproducibility, and explicitly stated uncertainty support defensible conclusions."],
      ["Which operating-system concepts must be combined for this investigation?", ["Processes, handles, memory, PE loading, security, synchronization, and IPC", "Only decimal arithmetic", "Only Registry values", "Only thread priority"], 0, "Runtime investigation requires an integrated model of process identity, resources, memory, loading, authorization, coordination, and communication."]
    ]
  }
};
